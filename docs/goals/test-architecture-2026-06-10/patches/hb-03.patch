diff --git a/crates/ferrum-sampler/src/guided.rs b/crates/ferrum-sampler/src/guided.rs
index 0078e26f..c86f4b0e 100644
--- a/crates/ferrum-sampler/src/guided.rs
+++ b/crates/ferrum-sampler/src/guided.rs
@@ -158,10 +158,8 @@ impl RegexGuidedProcessor {
     pub fn mask_logits(&self, logits: &mut [f32]) {
         let pos = *self.state.lock();
         if pos.dead {
-            // The generated prefix already left the DFA language. At this
-            // point a hard mask cannot repair the output; forcing EOS would
-            // actively truncate structured responses. Let generation proceed
-            // and leave final API/schema validation to reject bad output.
+            // Give up and force EOS — no other token can recover.
+            self.force_eos(logits);
             return;
         }
 
@@ -172,19 +170,9 @@ impl RegexGuidedProcessor {
         // keeps the hot path allocation-free for now.
         let vocab = logits.len().min(self.token_bytes.len());
         let mut any_allowed = false;
-        let mut fallback_token: Option<(usize, f32)> = None;
         for idx in 0..vocab {
             let is_eos = self.eos_token.map_or(false, |e| e.get() as usize == idx);
             let bytes = &self.token_bytes[idx];
-            let original_logit = logits[idx];
-            if !is_eos
-                && original_logit.is_finite()
-                && fallback_token
-                    .map(|(_, best)| original_logit > best)
-                    .unwrap_or(true)
-            {
-                fallback_token = Some((idx, original_logit));
-            }
 
             let allowed = if is_eos {
                 pattern_done
@@ -204,18 +192,12 @@ impl RegexGuidedProcessor {
             }
         }
 
-        // No token in the vocab can extend the current match. If the regex is
-        // already complete, EOS is the correct clean terminator. Otherwise do
-        // not force an invalid premature EOS; choose the model's best non-EOS
-        // token and let later API/schema validation reject the response if the
-        // fallback cannot recover. This can happen when single-token decode
-        // surfaces differ from full-sequence decode for byte-level tokenizers.
+        // No token in the vocab can extend the current match. Rather than
+        // hand the sampler a row of -inf (which produces NaN softmax and a
+        // junk argmax) terminate cleanly by forcing EOS. Happens when the
+        // tokenizer's BPE merges span byte boundaries the schema rejects.
         if !any_allowed {
-            if pattern_done {
-                self.force_eos(logits);
-            } else if let Some((token, _)) = fallback_token {
-                self.force_only_token(logits, token);
-            }
+            self.force_eos(logits);
         }
     }
 
@@ -228,12 +210,6 @@ impl RegexGuidedProcessor {
         }
     }
 
-    fn force_only_token(&self, logits: &mut [f32], token: usize) {
-        for (i, l) in logits.iter_mut().enumerate() {
-            *l = if i == token { 0.0 } else { f32::NEG_INFINITY };
-        }
-    }
-
     /// Public wrapper around `advance_with_tokens` for direct callers
     /// (the engine applies the mask inline rather than via
     /// `LogitsProcessor::process`).
@@ -383,86 +359,6 @@ mod tests {
         RegexGuidedProcessor::new(pattern, tok, Some(TokenId::new(256))).unwrap()
     }
 
-    struct TinyTokenizer {
-        special: SpecialTokens,
-        strings: Vec<String>,
-    }
-
-    impl TinyTokenizer {
-        fn new(strings: Vec<&str>, eos: u32) -> Self {
-            Self {
-                special: SpecialTokens {
-                    bos_token: None,
-                    eos_token: Some(TokenId::new(eos)),
-                    unk_token: None,
-                    pad_token: None,
-                    sep_token: None,
-                    cls_token: None,
-                    mask_token: None,
-                },
-                strings: strings.into_iter().map(str::to_string).collect(),
-            }
-        }
-    }
-
-    impl Tokenizer for TinyTokenizer {
-        fn encode(&self, text: &str, _add_special: bool) -> Result<Vec<TokenId>> {
-            Ok(text
-                .chars()
-                .filter_map(|ch| self.token_id(&ch.to_string()))
-                .collect())
-        }
-
-        fn decode(&self, tokens: &[TokenId], _skip_special: bool) -> Result<String> {
-            Ok(tokens
-                .iter()
-                .filter_map(|token| self.token_text(*token))
-                .collect::<Vec<_>>()
-                .join(""))
-        }
-
-        fn decode_incremental(&self, _prev: &[TokenId], next: TokenId) -> Result<String> {
-            self.decode(&[next], false)
-        }
-
-        fn vocab_size(&self) -> usize {
-            self.strings.len()
-        }
-
-        fn special_tokens(&self) -> &SpecialTokens {
-            &self.special
-        }
-
-        fn token_id(&self, text: &str) -> Option<TokenId> {
-            self.strings
-                .iter()
-                .position(|value| value == text)
-                .map(|idx| TokenId::new(idx as u32))
-        }
-
-        fn token_text(&self, token_id: TokenId) -> Option<&str> {
-            self.strings
-                .get(token_id.get() as usize)
-                .map(|value| value.as_str())
-        }
-
-        fn apply_chat_template(&self, _messages: &[ChatMessage]) -> Result<String> {
-            Ok(String::new())
-        }
-
-        fn info(&self) -> TokenizerInfo {
-            TokenizerInfo {
-                tokenizer_type: TokenizerType::Custom,
-                vocab_size: self.strings.len(),
-                special_tokens: self.special.clone(),
-                supports_incremental: true,
-                supports_chat_template: false,
-                max_token_length: Some(1),
-                model_name: Some("tiny-tokenizer-test".into()),
-            }
-        }
-    }
-
     #[test]
     fn digits_only_allows_digits_at_start() {
         let p = processor(r"[0-9]+");
@@ -499,33 +395,19 @@ mod tests {
     }
 
     #[test]
-    fn dead_state_disables_masking_instead_of_forcing_eos() {
+    fn dead_state_forces_eos() {
         let p = processor(r"[0-9]+");
         // Feed an invalid token ("a") — DFA dies.
         p.advance_with_tokens(&[TokenId::new(b'a' as u32)]);
         let mut logits = vec![0.0f32; 257];
-        logits[256] = 7.0;
-        p.mask_logits(&mut logits);
-        assert_eq!(logits[256], 7.0, "EOS should not be forced");
-        assert_eq!(logits[b'a' as usize], 0.0, "mask should be disabled");
-    }
-
-    #[test]
-    fn no_extension_before_accept_uses_best_non_eos_token() {
-        let tok: Arc<dyn Tokenizer> = Arc::new(TinyTokenizer::new(vec!["a", "b", "</s>"], 2));
-        let p = RegexGuidedProcessor::new("z", tok, Some(TokenId::new(2))).unwrap();
-        let mut logits = vec![0.1, 3.0, 99.0];
         p.mask_logits(&mut logits);
-
-        assert!(
-            logits[0].is_infinite(),
-            "lower non-EOS token should be masked"
-        );
-        assert!(logits[1].is_finite(), "best non-EOS token should remain");
-        assert!(
-            logits[2].is_infinite(),
-            "EOS must stay masked before the regex accepts"
-        );
+        assert!(logits[256].is_finite(), "EOS forced as fallback");
+        for b in 0u8..=255 {
+            assert!(
+                logits[b as usize].is_infinite(),
+                "byte {b} should be masked when DFA dead"
+            );
+        }
     }
 
     #[test]
