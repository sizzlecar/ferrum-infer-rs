# S1 CUDA Status - 2026-07-17

## Scope

This status records the current Qwen3.5-4B CUDA production slice at source commit
`f2c9a27752d3da453286387b385f882b5b8ded47`. It is evidence inventory, not an S1,
G01B, performance, or release PASS.

## Production Change

- Commit: `f2c9a27752d3da453286387b385f882b5b8ded47`
  (`refactor(vnext): stage prefill execution authority`), pushed to
  `origin/goal/runtime-vnext-0.8.0`.
- The admitted prefill state now retains the exact `SequenceSession`,
  `StepResourceLease`, and `PreparedStepSubmissionWave` selected by admission.
- Execution consumes that retained authority. Cancellation, error, and dropped-future
  cleanup use one RAII guard, and a live executing request id remains reserved until
  its submitted wave drains.
- Focused bounded local evidence:
  `/Users/chejinxuan/ferrum-artifacts/runtime-vnext-prefill-admission-d13998ae-20260716T205629Z`.
  It contains formatting, model-library compile/test, engine plan-runtime tests, and
  interface cancellation/wave contract tests, all run with bounded Rust test threads.

## CUDA Evidence

The complete copied artifact root is:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-s1-prefill-f2c9a277-20260717
```

The binary SHA256 is
`704176357e4b944a4fd5c0eeb9529ff75f215cdbb1cb23fe13667400fb11bafa`.
The hardware was one RTX 4090. Actual Qwen3.5-4B CUDA evidence includes:

- `ferrum run`: exit `0`, assistant output `Paris`.
- `ferrum serve`: HTTP `200`, streamed output `Paris`, exactly one `[DONE]`, and
  exactly one positive usage row.
- `ferrum bench-serve`: 4/4 requests completed, 0 failed, usage-derived output-token
  counts, and zero protocol/quality counters.
- Basic correctness/trace validation: PASS with 399 typed events for `run`, 2,394
  typed events for `serve`, and balanced resource ownership.

The capacity-pressure gate produced these exact PASS lines:

```text
FERRUM RUNTIME VNEXT S1 CUDA CAPACITY PRESSURE PASS: /workspace/ferrum-artifacts/runtime-vnext-s1-prefill-f2c9a277-20260717/capacity/gate
FERRUM GATE vnext-s1-cuda-capacity PASS: /workspace/ferrum-artifacts/runtime-vnext-s1-prefill-f2c9a277-20260717/capacity/gate
```

The validated dynamic-pressure facts are:

- exact runtime memory budget: `8,474,204,496` bytes;
- B entered `WaitForRelease` twice from `backing_maintenance`;
- unchanged-epoch skip count: `124`, below the limit of `512`;
- release epoch advanced from `379` to `869`;
- A remained active, later C completed, and deferred B resumed and completed;
- A/B/C emitted 128/128/16 content chunks respectively, each with usage and one
  `[DONE]`;
- pressure trace size: `3,164,654` bytes, below the 16 MiB limit.

## Rejected Performance Checkpoint

All eight ABBA-BAAB slots completed without request or correctness failures, but the
formal S1 basic-slice checkpoint correctly rejected the evidence:

```text
FERRUM RUNTIME VNEXT S1 CUDA BASIC SLICE FAIL: profile off CV 0.055423 exceeds 0.05
```

Measured values versus the Goal limits:

| Metric | Observed | Required | Result |
|---|---:|---:|---|
| profile-off CV | 5.542% | <= 5% | REJECT |
| profile-basic CV | 11.144% | <= 5% | REJECT |
| mean basic overhead | 0.688% | <= 2% | within limit, not accepted under noisy evidence |
| median basic overhead | 5.669% | <= 2% | REJECT |
| max basic trace bytes/request | 857,974.13 | <= 1 MiB | PASS |

Within each server process the repeats were stable, while slot means shifted between
roughly 30 and 39 output tokens/s. The current artifact does not record GPU clocks,
power state, temperature, or host load per slot, so it cannot distinguish collector
noise, host contention, or a real profile-mode effect. No performance claim is made.

## Current Gap And Next Stop Condition

S1 and G01B remain open. The immediate work is:

1. Replace the remote-only `/workspace/collect_s1_basic.sh` with a minimal checked-in
   canonical collector so the paid lane is reproducible; do not let collector work
   displace the S2 production milestone.
2. Record pre/during/post slot GPU P-state, clocks, power, temperature, utilization,
   memory, and host CPU/load evidence without changing the product runtime command.
3. Run the next paid performance diagnostic only after collector self-tests pass. The
   expected signal is that hardware-state regimes explain the 30-39 token/s clusters;
   reject the run without another source change if either profile group still exceeds
   5% CV or hardware state remains unstable.
4. Build G01B from the same G00F, G01A, S1 correctness/capacity, contract-source, five
   extension-drill, deterministic-plan/schema, zero-legacy-fallback, and accepted
   overhead evidence. Capacity PASS alone cannot sign G01B.

The checked-in collector is now `scripts/release/runtime_vnext_s1_cuda_basic_collector.py`.
It fixes the workload and ABBA-BAAB order, runs the correctness checkpoint before any
performance slot, and records non-mutating GPU/host telemetry before, during, and after
each slot. The validator no longer encodes the historical first-half REJECT as a future
acceptance requirement. This source change is still pending a fresh one-RTX-4090 raw
artifact and therefore does not close the rejected performance checkpoint by itself.

At that checkpoint, paid GPU inventory after artifact copy-back was zero; no Vast
instance was retained or billing.

## Batch Decode CUDA Checkpoint At `642a9904`

Commits `266fe30e` and `642a9904` moved PlanRuntime decode from one submitted wave
per request to one canonical multi-participant wave with terminal multi-readback. The
engine now calls `batch_decode` once for a ready decode cohort and validates output
cardinality and cache identity before committing any host token or recurrent state.

The complete copied artifact root is:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-plan-batch-cuda-642a9904-20260717
```

The tested source SHA was
`642a990417bcc5eb968afac9ad4b05a15565f82a`. The diagnostic used a public
`--features cuda` release binary with SHA256
`ffbad1abc0416319b4c82cf9dc9a00b79be5b372b85348ba1b1249dea780c209` on one
RTX 4090. It did not use product behavior hidden behind environment variables.

Actual Qwen3.5-4B CUDA correctness passed before the concurrency cells:

- `ferrum run` exited `0` and produced `Paris`;
- `ferrum serve` returned HTTP `200`, produced `Paris`, emitted one positive usage
  row and exactly one `[DONE]`;
- all c1/c4/c16 requests completed with usage-derived token counts, zero request
  errors, zero protocol/quality counters, and zero failed waves;
- terminal active, pending, staged, maintenance, and executing counts were all `0`.

The bounded diagnostic printed:

```text
FERRUM RUNTIME VNEXT CUDA BATCH DECODE DIAGNOSTIC PASS: /workspace/ferrum-artifacts/runtime-vnext-plan-batch-cuda-642a9904-20260717/diagnostic
```

This is a diagnostic PASS, not the formal S1, G01B, performance, or release PASS.
The observed cohort facts were:

| Cell | Requests | Decode participants | Decode waves | Participants/wave | Output tok/s |
|---|---:|---:|---:|---:|---:|
| c1 | 4/4 | 155 | 155 | 1.00 | 26.02 |
| c4 | 8/8 | 279 | 155 | 1.80 | 26.45 |
| c16 | 32/32 | 1,023 | 156 | 6.56 | 34.06 |

The structural candidate is KEEP because actual c4/c16 requests shared fewer waves
without state contamination. No performance claim is accepted: c16 delivered only
`1.31x` c1 output throughput at `16x` concurrency, with c16 p95 TTFT `9,801.56 ms`
and p95 TPOT `381.33 ms`. The source and trace shape indicate that control-plane wave
coalescing is complete while CUDA providers still launch participant-local work; that
is the next profile hypothesis, not yet a proven kernel bottleneck.

The same host's official feature command was stopped after `1,542s`, already beyond
the G07 clean-release ceiling of `900s`, and did not produce a binary. Its wrapper
incorrectly reported exit `0`; the missing binary invalidates that receipt. The saved
verdict is `clean_official_cuda_build_exceeds_goal_ceiling_and_has_no_binary`.

Instance `45123501` was verified `cur_state=stopped` and
`actual_status=exited` after artifact copy-back. It is retained for at most four hours
at `$0.0833/h` stopped storage cost because it contains the pinned 8.8 GiB model and
the successful core CUDA cache. It must not be resumed until a narrow source
hypothesis and bounded local gate exist.

The next source stop condition is a generic execution-capacity wait contract used by
both prefill and decode. Decode capacity exhaustion must subscribe before yielding,
perform zero hot-loop admission probes while epochs are unchanged, and resume only
after an exact release/capacity source advances. After that local gate passes, collect
provider launch-shape evidence before changing CUDA kernels or starting another paid
performance run.

## Typed Decode Capacity Checkpoint At `2bbda7cb`

Commit `2bbda7cbb1f8bfcca915b71b9e67db36ce5566e8`
(`refactor(vnext): park decode on typed capacity`) is pushed to
`origin/goal/runtime-vnext-0.8.0`. It closes the source-level stop condition above:

- sequence extension, step admission, and submission-wave pressure return a typed
  pre-submit deferral instead of a stringly resource error;
- the engine adaptively splits a pressured cohort so an oversized cohort does not
  block independent requests that still fit;
- a single blocked decode remains active with its exact capacity-source predicate and
  is not reprobed while those source generations are unchanged;
- new work and shutdown interrupt the capacity wait without blindly reprobeing old
  work;
- a real failure after a split is owned by its exact subcohort and cannot complete an
  already-successful sibling request.

The clean-SHA focused artifact is:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-typed-decode-capacity-2bbda7cb-20260717T074229+0800
```

Its `checkpoint-verdict.json` records `KEEP`, `formal_s1_pass=false`, and
`formal_performance_claim=false`. The bounded evidence contains a four-crate library
check, format/diff checks, and 11 focused tests with zero failures: four batch-decode
contracts, one exact-subcohort failure test, two engine capacity-wait tests, one
scheduler exact-source wake test, and three vNext executor resource-state tests.

Vast instance `45123501` was destroyed after its bounded cache-retention deadline.
The reconciled paid inventory is empty, so no running, scheduling, or stopped instance
is accruing cost.

The next stop condition is a checked-in narrow CUDA diagnostic that deliberately
provokes decode-side sequence-extension, step, or wave pressure and requires the typed
defer trace, zero unchanged-source reprobes, eventual completion after exact-source
advance, and zero terminal resource leaks. Only after its local/self-test passes may
one RTX 4090 be rented for that diagnostic. This checkpoint does not authorize a
performance claim or a provider/kernel change.

## Cross-Phase Capacity REJECT At `da9c1ee8`

The checked-in decode-capacity collector was rerun on one RTX 4090 against
Qwen3.5-4B at source SHA
`da9c1ee8363c686e71420fd5df8042c496e69757`. The release binary SHA256 was
`19fe1907e1d74c199fb34da4990297109e5e05257600f1299426f3e9eb6d50c4`.
The copied artifact root is:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-s1-progress-lease-da9c1ee8-20260717
```

The collection produced no PASS line and is classified
`cross_phase_capacity_progress_deadlock`:

- the typed lease released correctly with `baseline=49`, `current=50`,
  `decision=owner_advanced`, and `probe_performed=false`;
- A/B/C produced `81/33/16` content chunks, but only C emitted `[DONE]` and usage;
- B exceeded the no-progress bound at `30.010s`;
- the final scheduler snapshot had `active=2`, `blocked prefill=1`, and
  `blocked decode=1`;
- both blocked phases depended on capacity domain `4` generation `73`, while no
  pending release/yield transaction could advance that source;
- 64 unchanged-prefill observations performed zero probes but demonstrate that
  per-tick diagnostic emission is not an acceptable steady pressure representation.

This disproves the assumption that releasing a phase-local admission lease after one
owner generation is sufficient. The resource liveness authority is split across
waiting, prefill, and decode queues, so a recompute request can leave the decode-only
victim selector and later deadlock with its prior owner.

The replacement direction is a single typed logical work frontier plus a cold-path
pressure transaction over the live running set. Prefill, recompute, decode, and
speculative work remain executor work kinds; they do not own separate capacity
liveness state. The no-pressure path must keep stable batch membership, allocate
nothing, and create no pressure episode. At a real allocation failure, one scheduler
transaction must either claim work for at least one frontier or plan a typed
yield/fence-delayed release before parking.

Vast instance `45148490` was stopped only after artifacts were copied and verified.
Its state is `cur_state=stopped`, `actual_status=exited`; the 150 GB disk retains the
8.8 GiB HF model and CUDA build cache. Stopped storage is estimated at `$0.0417/h` and
has a hard retention deadline of `2026-07-17T11:52:43Z` (`<=$0.167`); resume it only
after the CUDA trace is a deterministic local replay, the final
`1 prefill + 1 decode` shared-source state is killed by a bounded scheduler/engine
test, and ordered transition validation passes. Otherwise destroy it at the deadline.
This checkpoint is a REJECT and proves no S1, G04, performance, or release goal.

## Cross-Phase Pressure Source Candidate At `0b91f422`

Commit `0b91f422` (`refactor(vnext): coordinate cross-phase capacity pressure`)
is pushed to `origin/goal/runtime-vnext-0.8.0`. It replaces the phase-local
liveness assumption disproved by `da9c1ee8` with:

- one phase-independent logical work frontier for prefill, recompute, and decode;
- a cold-path pressure coordinator indexed by exact capacity source and request;
- ordered `YieldPlanned -> AwaitReleaseFence -> Resumable -> Closed` transactions;
- request/cache-identified executor preemption for both retained prefill and active
  sequence authority;
- an atomic `SequenceSession::try_abort_if_quiescent` transition, so a rejected
  preemption cannot leave a half-cancelled session;
- exact-source advancement verification before the scheduler can make a progress
  owner runnable;
- typed abort/reconciliation for every intermediate release failure; and
- terminal-owner handling that resolves the fence before closing the episode and
  never resubmits a cancelled owner.

The bounded local source evidence passed:

- four-crate library `cargo check`;
- scheduler library `120/120`;
- engine PlanRuntime `16/16`;
- atomic quiescent-abort contract `1/1` with `RUST_TEST_THREADS=1`;
- vNext executor model tests `3/3`; and
- `FERRUM RUNTIME VNEXT S1 CUDA DECODE CAPACITY SELFTEST PASS`.

This is a source candidate, not actual CUDA evidence. The latest real CUDA result
remains the `da9c1ee8` REJECT artifact above, and no S1, G04, performance, or release
PASS is claimed.

The retained instance `45148490` exceeded its bounded cache-retention deadline. Its
REJECT artifacts had already been copied back, so it was destroyed through the Vast
API. The follow-up inventory returned `instance_count=0`; no running, scheduling, or
stopped instance is accruing cost.

The next paid lane is one RTX 4090 `vnext-s1-cuda-decode-capacity` diagnostic at exact
SHA `0b91f422`. Its correctness gate is the checked-in run/serve/capacity scenario and
its performance command is intentionally omitted because this lane makes no
performance claim. Stop after the first canonical PASS/REJECT artifact, any recurrence
of the same cross-phase deadlock, failure to advance the exact source after a typed
release receipt, or the lane timeout. Copy artifacts back and stop or destroy the
instance before any subsequent source change.

## Source-Causal Pressure REJECT At `3d434406`

The checked-in decode-capacity collector was rerun on one RTX 4090 against
Qwen3.5-4B at source SHA
`3d434406e8d7025b6981a1e6d419d66a5d73af74`. The release binary SHA256 was
`456c62906a02a0dc47b18607d85423eb4853aef21843acf3a91cc08850089d06`.
The copied artifact root is:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-s1-pressure-causal-3d434406-20260717
```

The product `run` check, serve calibration, and target-capacity sizing completed,
but the target A/B stream cell produced no PASS line. The collector failed with:

```text
FERRUM RUNTIME VNEXT S1 CUDA DECODE CAPACITY FAIL: /workspace/artifacts/runtime-vnext-s1-pressure-causal-3d434406-20260717/raw: stream token progress timeout exceeded for role B: 30.010s >= 30.000s
```

Request C completed while A and B remained open. The final scheduler state contained
two disjoint waits, `Domain(4)@136` and `Domain(2)@176`, two active requests, two
blocked prefill frontiers, two active pressure episodes, zero pending release fences,
and no allocator OOM, panic, or terminal resource leak. This is classified
`single_frontier_self_release_deadlock`: the coordinator knew only that another
request was generically releasable, not whether releasing it could advance the exact
source blocking the current request. That generic Boolean suppressed the blocked
owner's self-recompute even when the peer owned a disjoint capacity domain.

This checkpoint is a REJECT and proves no S1, G04, performance, or release goal.

## Exact Release Footprint Candidate At `befed131`

Commit `befed131963eb8cb41c270f01789971081e354ab`
(`fix(vnext): make pressure release source-causal`) is pushed to
`origin/goal/runtime-vnext-0.8.0`. It fixes the invalid generic-release assumption at
the ownership boundary:

- the live executor authority reports the exact capacity sources that releasing a
  request/cache identity can advance;
- the scheduler compares that release footprint with the current exact wait
  condition and does not infer release capability from request state;
- a causal peer gets a typed peer-handoff transaction; a disjoint peer cannot
  suppress the current owner's typed self-recompute;
- the engine releases the exact physical authority, resolves its fence, verifies
  source advancement, and only then re-admits logical work; and
- deterministic tests cover causal peer handoff, disjoint-domain self-recompute, and
  exact cache release before requeue.

The bounded local source evidence passed with thread caps:

- scheduler library: `126 passed, 0 failed`;
- engine self-recompute contract: `1 passed, 0 failed`;
- engine peer-handoff contract: `1 passed, 0 failed`; and
- `FERRUM RUNTIME VNEXT S1 CUDA DECODE CAPACITY SELFTEST PASS`.

This remains a source candidate until the real CUDA artifact is validated. The
falsifiable prediction is that every open pressure episode either has a pending
release fence whose exact footprint intersects its wait condition or immediately
chooses self-recompute. The previous terminal shape, two disjoint open episodes with
zero fences, must not recur.

Paid inventory was reconciled before this run. Instance `45157630` is the only Vast
instance, one RTX 4090 at `$0.375/h`, and is `cur_state=running`,
`actual_status=running`. The lane is `vnext-s1-cuda-decode-capacity`, expected runtime
is 20-40 minutes and expected cost is `$0.13-$0.25`; there is no performance command.
Stop on the first canonical PASS/REJECT artifact, build failure, target timeout, or
recurrence of the same failure class. Copy the complete artifact back, then stop the
instance and verify `actual_status=exited` before another source change.

## Premature Peer-Hold Release REJECT At `befed131`

The canonical collector ran on the retained RTX 4090 at exact source SHA
`befed131963eb8cb41c270f01789971081e354ab` with release binary SHA256
`ad0a48a24c1633ead6f97366f2d482a6a92a9ef73b4803fd2df7b586f39945ff`.
The complete copied artifact root is:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-s1-release-footprint-befed131-20260717
```

The build, `ferrum run`, calibration A/B/C, and target-sizing A/B/C completed. The
target collector returned `1`, the gate was correctly skipped with `125`, and no PASS
line was produced. The exact collector failure was:

```text
FERRUM RUNTIME VNEXT S1 CUDA DECODE CAPACITY FAIL: /workspace/artifacts/runtime-vnext-s1-release-footprint-befed131-20260717/raw: pressure trace byte limit exceeded: 16858374 > 16777216
```

The byte ceiling exposed scheduler churn rather than a validator-only size issue. In
9.5 seconds the target emitted 10,053 trace rows and reached 17,025,326 bytes. The
first peer handoff completed at scheduler iteration `56`; its victim was re-admitted
at iteration `57` as soon as the owner committed token progress. The roles reversed
at iteration `88`, after which the same retained-prefill request performed 18
`self_recompute` transactions at iterations `91..125` without advancing its logical
work generation. This rebuilt 23 vNext plans for three product requests. C completed;
A/B had `81/33` content chunks but no usage or `[DONE]` when the bounded collector
stopped. There was no CUDA OOM, panic, malformed stream, or unsettled client cleanup.

This is classified
`peer_hold_released_before_capacity_progress_causing_recompute_rotation`. Logical
token progress consumes capacity and is not evidence that a yielded victim fits.
Therefore peer-handoff holds must remain active until the selected owner terminates
and releases resident authority, unless a future typed reservation proves that the
victim now fits. Raising the trace ceiling or suppressing events would hide the
resource-policy defect and is explicitly rejected.

The next source prediction is exact: after the first peer handoff, `admitted_total`
must remain unchanged until owner terminal; reverse peer handoffs and repeated
retained-prefill self-recompute must both be zero. The CUDA target must complete A/B/C
with usage and exactly one `[DONE]` each while remaining below the existing 16 MiB
trace ceiling. Instance `45157630` is retained running only until
`2026-07-17T23:15:00+08:00` for one locally gated incremental follow-up, capped at
about 43 minutes and `$0.27` additional cost. If the local gate is not ready by that
deadline, stop the instance without another paid run.

## Held-Victim Promotion REJECT At `0036b89a`

The bounded follow-up ran on the same RTX 4090 at exact clean source SHA
`0036b89a3f96667a9f764f7dc4c9839fe6da215d`. The release binary SHA256 was
`b06127790c91e53e854d27e82e4581214e36b7e8f405a5081942e771917357fc`. The complete
copied artifact root is:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-s1-active-first-0036b89a-20260717
```

The prior trace explosion was removed: the target trace fell from 10,053 rows and
17,025,326 bytes to 2,686 rows and 4,305,791 bytes. The collector still produced no
PASS line and failed at the bounded progress timeout:

```text
FERRUM RUNTIME VNEXT S1 CUDA DECODE CAPACITY FAIL: /workspace/artifacts/runtime-vnext-s1-active-first-0036b89a-20260717/raw: stream token progress timeout exceeded for role B: 30.008s >= 30.000s
```

A/B/C emitted `113/33/16` content chunks. Only C emitted usage and exactly one
`[DONE]`; A and B remained open. The pressure journal proves an ownership defect
rather than a trace-volume issue:

- episode `1` first selected `19ec...` as progress owner and `774a...` as held peer;
- a later failure in the same episode selected the prior held `774a...` as owner and
  yielded `19ec...`, despite no admission claim transferring ownership;
- the old implementation then emitted `role_transferred` for `774a...` and
  `source_retargeted` for `19ec...`;
- the terminal snapshot had two active requests, two blocked recompute-prefill
  frontiers, two active pressure episodes, and zero pending release fences.

This is classified `held_victim_promoted_without_admission_claim`. It disproves the
assumption that the most recent transaction victim can also represent the durable
progress owner. Shutdown after the timeout logged two request-slot ownership warnings;
therefore this artifact is not terminal-clean evidence and proves no S1, G04,
performance, or release goal.

Instance `45157630` was stopped after the artifact was copied. The reconciled state is
`cur_state=stopped`, `actual_status=exited`; it is the only retained Vast instance and
keeps the Qwen3.5-4B model plus CUDA build cache. It must not be resumed without a
clean source candidate and the exact owner-stability gate below.

## Admission-Causal Stable Owner Candidate At `baa4e8ce`

Commit `baa4e8ce` (`fix(vnext): make pressure ownership admission-causal`) is pushed
to `origin/goal/runtime-vnext-0.8.0`. It replaces mutable role rotation with one
explicit pressure state machine:

- `progress_owner`, held participants, and `last_transaction_victim` are separate;
- a live owner remains stable across logical progress and wait-source retargeting;
- a self-recomputing owner with held peers enters `OwnerAdmissionPending` and becomes
  resumable only after the executor returns a typed admission receipt;
- held peers remain ineligible until owner terminal; `role_transferred` and
  `source_retargeted` release dispositions no longer exist;
- an unclaimable recompute owner produces `OwnerRecomputeBlocked` rather than silent
  peer promotion; and
- engine evidence includes the owner-admission-pending transition after the second
  exact physical cache release.

The clean-SHA bounded artifact is:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-stable-pressure-owner-baa4e8ce-20260717T234654+0800
```

It contains `212/212` engine tests, `128/128` scheduler tests, formatting, and the
exact validator line:

```text
FERRUM RUNTIME VNEXT S1 CUDA DECODE CAPACITY SELFTEST PASS
```

The CUDA prediction is falsifiable. All yields in one episode must retain one owner;
neither legacy release disposition may occur. If the owner releases itself while a
peer is held, the fence must end as `progress_owner_admission_pending`, the owner may
be admitted only after a typed receipt, and the peer must receive zero admission
probes until owner terminal. A/B/C must each finish with usage and exactly one
`[DONE]`, and the target trace must remain below 16 MiB. Failure to claim a recompute
owner must surface `OwnerRecomputeBlocked` as an explicit REJECT instead of another
30-second timeout. This is still a source candidate; actual CUDA has not yet passed.

## Owner-Recompute Capacity REJECT At `12e461ac`

The canonical decode-capacity collector ran on one RTX 4090 at source SHA
`12e461ac9210acaab3f39979d5f25b5df53c6476` with binary SHA256
`968c2ed482db3cd523b49a6007ca6acbe99e7ea4a308ee434a0348030bbf2778`. The copied
artifact root is:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-s1-stable-owner-12e461ac-20260718
```

The stable-owner state machine prevented held-peer promotion, but the target still
REJECTED after B made no token progress for `30.008s`. A and C completed with
`113/16` tokens, usage, and exactly one `[DONE]`; B stopped at `33` content chunks
without usage or `[DONE]`. The server surfaced the intended typed invariant instead
of silently rotating ownership:

```text
prefill execution-capacity pressure episode 1 violated OwnerRecomputeBlocked
```

The source/artifact diagnosis was physical residency fragmentation across typed
pools: the blocked recompute pool could not grow under the plan budget while whole,
idle, unreferenced chunks remained resident in other pools. The runtime had no
plan-owner transaction that could transfer that physical budget. This is a G04
resource-architecture gap, not a scheduler retry-policy defect.

## Cross-Pool Residency Candidate At `21461c95`

Commit `21461c9574a411917d0c1a0eb88e579d3a5fbd4e`
(`fix(vnext): rebalance idle dynamic pool residency`) is pushed. It adds one bounded
plan-owner transaction that:

- locks pools in canonical order and selects the complete reclaim batch before mutation;
- reclaims only whole chunks with zero live segments and no external buffer authority;
- never takes a target-domain chunk or crosses the plan minimum/logical-use floor;
- publishes logical totals and capacity epochs atomically with rollback on failure;
- drops physical grants only after all pool locks are released; and
- retries the blocked growth once and emits a typed aggregate receipt into the engine trace.

The clean local artifact is:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-dynamic-rebalance-21461c95-20260718T021227+0800
```

It printed:

```text
FERRUM RUNTIME VNEXT DYNAMIC REBALANCE LOCAL PASS: /Users/chejinxuan/ferrum-artifacts/runtime-vnext-dynamic-rebalance-21461c95-20260718T021227+0800
```

This is source evidence only. It does not prove the CUDA production path, S1, G04,
performance, or release.

## Global-Budget CUDA Diagnostic At `21461c95`

The follow-up CUDA run used one RTX 4090 and release binary SHA256
`870391a958692b301460b0a75a3fbc3a1a1a64ddeee30a4551a3f0ce14cd8653`. The complete
artifact and machine-lifecycle root is:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-s1-cross-pool-rebalance-21461c95-20260718
```

`ferrum run` succeeded. Target A/B/C completed `128/128/16` tokens; every stream had
usage, exactly one `[DONE]`, and no error. The collector still correctly produced no
PASS line. It rejected a target pool that exceeded its per-pool sizing maximum, while
the target's total installed backing remained below the configured global budget.
The target also produced zero decode-capacity deferrals and zero rebalance receipts,
so relaxing that validator alone would have created false evidence.

The old derivation summed independent per-pool maxima into `8,501,046,736` bytes. The
target-sizing server actually installed `8,474,701,264` bytes. Their
`26,345,472`-byte difference is exactly one recurrent-state chunk and removed the
pressure that this lane must test. Commit `f8081bc5`
(`test(vnext): calibrate cross-pool CUDA pressure`) therefore changes the contract to
the target-sizing global installed budget. Individual pools may redistribute within
that total, but the gate now requires both typed decode pressure and a typed
cross-pool reclaim receipt.

The next paid run has one falsifiable prediction:

- exact target budget: `8,474,701,264` bytes;
- at least one `vnext.decode_capacity_deferred` event;
- at least one maintained prefill event with non-zero `pools_reclaimed`,
  `chunks_reclaimed`, and `reclaimed_bytes`;
- target final physical claim no greater than the exact budget; and
- A/B/C all complete with usage and exactly one `[DONE]`.

The focused validator self-test, `run_gate.py --self-test`, validator suite, and diff
check passed before `f8081bc5` was pushed. This remains a candidate until a fresh CUDA
artifact prints the exact S1 decode-capacity PASS line. Instance `45187567` was
destroyed after all 62 raw files matched their copied SHA256; the post-destroy Vast
inventory was empty.

## Pool-Ceiling CUDA REJECT At `f4df2370`

The next clean one-RTX-4090 run used source SHA
`f4df23707be47031a1b428f49a1b268228accc7f` and release binary SHA256
`1eedc7ee1cba1105d025907cb85706ea8fafede848e5692958d9301d7d6091f7`. The complete
copied artifact root is:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-s1-pool-ceiling-f4df2370-20260718
```

The exact runtime budget was `8,474,701,264` bytes. Product correctness was clean:
A/B/C completed `128/128/16` tokens, each emitted one usage row and exactly one
`[DONE]`, the server exited `0`, and final active/queued/pending/failed-wave counts
were all zero. The trace contained three decode-capacity deferrals and nine typed
prefill-maintenance events. The collector still produced no PASS line:

```text
FERRUM RUNTIME VNEXT S1 CUDA DECODE CAPACITY FAIL: pressure victim 3cec242b-57e7-4481-ac2d-5e6bd52f3559 was re-admitted before its owner released capacity
```

This was an artifact-ordering defect, not an admission-ordering defect. The owner
completed before the victim admission, and the victim admission snapshot already had
zero active pressure episodes. The engine then serialized the admission event before
the scheduler's preceding hold-release callback, reversing their causal order by
`12,754 ns`. The same artifact also exposed a separate unmet requirement: all nine
maintenance receipts had zero reclaimed pools/chunks/bytes.

All 72 copied remote files matched `sha256sum.txt`. Instance `45193228` was destroyed;
`vast-inventory-after-destroy.json` contains an empty inventory. The verdict is recorded
in `cuda-reject-verdict.json`. This artifact is a REJECT and proves no S1, G01B, G04,
performance, or release goal.

The fresh CUDA build also exposed a G07 compile bottleneck: vLLM Marlin/MoE CUDA
translation units were compiled serially by `ferrum-kernels/build.rs`, followed by
workspace release LTO with one codegen unit. Increasing `CARGO_BUILD_JOBS` did not
parallelize that `cicc` section. This observation is diagnostic only; G07 remains open.

## Causal Trace Serialization Candidate At `325f9dfe`

Commit `325f9dfe3ffc268e2913c39ef548f8a1de42295a` is pushed. Scheduler queue
observations and executor admission events now share one ordered trace-record buffer,
so the writer preserves callback order rather than concatenating two independently
captured vectors. A regression fixes the exact order to
`pressure_hold_released -> admission_probe`. Focused scheduler validation passed `1/1`,
and the engine library passed `212/212`. No CUDA claim is made from those local tests.

## Isolated Rebalance Probe Candidate At `c1e4ea08`

Commit `c1e4ea087f386557126cfa842ea951ee96c32831` is pushed. The decode-capacity
collector no longer asks the A/B/C window to accidentally create two unrelated facts.
On the same target server it now performs:

1. a target-sizing replay that must leave quiescent dynamic residency exactly
   `63,189,456` bytes and total claimed bytes exactly `8,474,701,264`;
2. a separate long-prompt, one-token streaming probe whose request-shaped growth must
   emit a positive typed cross-pool reclaim receipt; and
3. the original A/B/C pressure window, whose decode state machine is validated
   independently.

The sizing snapshot predicts two idle `26,345,472`-byte sequence chunks, four idle
`496,640`-byte request chunks, ten idle `524,288`-byte paged chunks, and only a
`640`-byte largest contiguous input chunk at exact budget saturation. The probe repeats
the vocabulary token `token` 256 times; the checked-in Qwen3.5 tokenizer contains
`Ġtoken` as one BPE token. Therefore the probe must outgrow the input chunk while
remaining below `max_model_len=512`, and the rebalancer must transfer an idle non-target
chunk before retrying request admission. The subsequent A/B/C window must still produce
at least one typed decode deferral and complete `128/128/16` with usage and one `[DONE]`
per role.

Local Python compilation, the decode-capacity self-test, the existing capacity
self-test, `run_gate.py --self-test`, and `git diff --check` passed before the commit.
This is a falsifiable source/scenario candidate only. No paid instance is currently
running, and no S1, G04, performance, or release PASS is claimed.

## Rebalance-Probe CUDA REJECT At `99f67026`

The exact-budget follow-up used one RTX 4090 and release binary SHA256
`d80565ec36503198aebd746c6606f0859a3cdbc54db6b4eafa1ed213d0c5168e`. Cross-pool
maintenance reclaimed one `491,520`-byte chunk and grew the input pool by `1,152`
bytes, so the intended typed rebalance path executed. The collector still correctly
rejected the run:

```text
FERRUM RUNTIME VNEXT S1 CUDA DECODE CAPACITY FAIL: stream did not produce a first-content signal
```

The target request then made no progress for `30.000809` seconds. Its scheduler
snapshot had one active request, one blocked prefill, one active pressure episode,
zero pending release fences, and wait observations for the step domain and plan device
budget. The only live request was waiting for capacity that only its own terminal
release could advance. The complete copied artifact is:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-s1-rebalance-probe-99f67026-20260718/cuda-reject-45196388
```

All remote evidence was copied before instance `45196388` was destroyed; the
post-destroy Vast inventory was empty. A preceding fresh instance, `45195867`, was
also destroyed after a setup-network REJECT and produced no correctness claim.

## Adaptive Prefill Frontier Candidate At `0c8498c5`

Commit `0c8498c55e5063a0c34c755c521c0643caae0cbc` replaces the lost executor/scheduler
capacity edge with an explicit partial-prefill contract:

- the scheduler publishes a maximum planned frontier;
- before provider encode or device submission, the executor may probe strictly
  narrower typed prefixes;
- completion returns both planned and completed chunks, and the engine commits only
  the completed prefix;
- the scheduler lowers its request-local execution ceiling after a failed probe and
  grows it again after successful exact prefixes; and
- sequence backing uses monotonic coverage semantics, so a previously reserved wider
  frontier can safely back a narrower execution prefix without reallocating or
  pretending that resource ownership regressed.

A one-token frontier with no external capacity releaser now fails closed instead of
installing an unproductive wait. The local contract artifact is:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-prefill-frontier-local-20260718T062231+0800
```

Its exact line is:

```text
FERRUM RUNTIME VNEXT PREFILL FRONTIER LOCAL PASS: /Users/chejinxuan/ferrum-artifacts/runtime-vnext-prefill-frontier-local-20260718T062231+0800
```

This remains local prediction evidence only. The next clean CUDA run must show a
`scheduler_prefill_execution_frontier_narrowed` event followed by first content within
the collector timeout, or an explicit one-token resource error; it must not reproduce
the single-owner passive wait. The official collector and validator still must print
their S1 PASS lines before S1 or G04 can advance.

## Operation-View CUDA REJECT At `12fadb2d`

The clean one-RTX-4090 follow-up used source SHA
`12fadb2dbf69cf505b63e94ef15e46cd7ac35206` and release binary SHA256
`b8cb2df90ebb794abb400a404a5e9619f4d05097bb483a6c0227a34d26e3cdfc`.
The complete copied artifact is:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-s1-prefill-frontier-12fadb2d-20260718/cuda-reject-45199891
```

`ferrum run` returned `Paris` with two tokens. Calibration, target sizing, and the
rebalance-prime A/B/C streams completed with positive usage and exactly one `[DONE]`.
The long-prompt probe also exercised the intended resource path: it reclaimed one
`491,520`-byte idle chunk from another pool and grew the blocked input pool by `1,152`
bytes. The previous 30-second single-owner wait did not recur. The collector instead
rejected the run immediately:

```text
FERRUM RUNTIME VNEXT S1 CUDA DECODE CAPACITY FAIL: /workspace/artifacts/runtime-vnext-s1-prefill-frontier-12fadb2d-20260718/raw: target-rebalance-probe: missing positive completion token count
```

The probe returned HTTP 200 in under 50 ms, but emitted `finish_reason="error"`, zero
content tokens, one usage row, and one `[DONE]`. The exact server failure was:

```text
operation provider failed with cuda.causal_paged_attention.encode: causal attention state is not its admitted fixed-block paged view
```

The failure exposed a core ownership/projection defect. Sequence authority retained a
wider backing allocated for the previously admitted frontier, while the narrowed
operation still published that full backing extent as its exact provider view. The
CUDA provider correctly rejected the mismatch between the current source frontier and
the advertised physical state. This is not a CUDA-kernel assertion to relax.

Collector exit was `1` and validator exit was `125`; no PASS line was produced.
Instance `45199891` was destroyed after evidence collection. The copied
`inventory-after-destroy.json` reports zero instances, so no paid GPU remained.

## Exact Operation Prefix Candidate At `b0d165c8`

Commit `b0d165c8461babb07bb05409f6f9fd561d858a40` separates retained backing capacity
from the exact resource view exposed to one operation. Sequence backing remains
monotonic and may be wider, but the operation descriptor is evaluated at the current
cumulative `source.end` frontier. Physical translation clips paged or contiguous
bindings to that exact prefix; insufficient backing still fails closed, and non-sequence
resources retain exact-coverage validation.

The focused local artifact is:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-operation-prefix-local-20260718T074548+0800
```

Its tested diff SHA256 exactly matches `git diff 12fadb2d b0d165c8`:
`51a295689d14305f4fb4be5bf4ced11c9b8abdfc8793cb0eda5c1b1c24581a90`.
Five-crate compilation, six operation-buffer unit contracts, two device-operation
batch contracts, sequence backing extension, scheduler frontier narrowing, and both
engine partial-prefill contracts passed. The exact local line is:

```text
FERRUM RUNTIME VNEXT OPERATION PREFIX LOCAL PASS: /Users/chejinxuan/ferrum-artifacts/runtime-vnext-operation-prefix-local-20260718T074548+0800
```

This is still prediction evidence, not CUDA, S1, performance, G04, or release evidence.
The next clean CUDA run must preserve the positive cross-pool reclaim receipt, remove
the fixed-block operation-view error, emit positive probe content and usage with exactly
one `[DONE]`, and finish with:

```text
FERRUM RUNTIME VNEXT S1 CUDA DECODE CAPACITY PASS: <out_dir>
```

## Request-Journal CUDA REJECT At `b0d165c8`

The clean one-RTX-4090 follow-up used source SHA
`b0d165c8461babb07bb05409f6f9fd561d858a40` and release binary SHA256
`35802837fa4984805d9b86ca6885715d553bd2bdea0fddcffee2e8c96f6c2de7`.
The complete copied artifact is:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-s1-operation-prefix-b0d165c8-20260718/cuda-reject-45202825
```

`ferrum run` returned `Paris`; target sizing and rebalance-prime A/B/C each emitted
positive usage and exactly one `[DONE]`. The probe again completed the typed backing
maintenance: one pool reclaimed an idle chunk and the blocked pool grew by the exact
`1,152` bytes. The prior fixed-block operation-view error disappeared. The probe then
returned HTTP 200 in about 86 ms with one usage row and one `[DONE]`, but zero content
tokens and `finish_reason="error"`. The exact server failure was:

```text
Plan-runtime prefill failed for ea571d1a-620b-4f7e-8025-447263dab645: Backend error: vNext execution event emission failed: invalid execution plan: request journal run, request, sequence, timestamp, phase, or terminal boundary is invalid
```

The collector printed:

```text
FERRUM RUNTIME VNEXT S1 CUDA DECODE CAPACITY FAIL: /workspace/artifacts/runtime-vnext-s1-operation-prefix-b0d165c8-20260718/raw: target-rebalance-probe: missing positive completion token count
```

Source analysis and a local failing reproduction identified the violated dimension as
the execution sequence/frame boundary. A capacity-deferred pristine Step advanced the
sequence session from frame 1 to frame 2 even though no invocation or physical operation
had been submitted. The maintained retry therefore built its first operation identity
from frame 2, while the request journal had only `RequestAccepted=1` and `PlanBuilt=2`
and still required the first physical frame at rows `3/4/5`. Relaxing the journal would
hide a real gap and contradict the contiguous-frame replay contract.

Collector exit was `1` and validator exit was `125`; no PASS line was produced. All 89
copied evidence files passed SHA256 verification. Instance `45202825` was destroyed after
artifact collection, and `inventory-after-destroy.json` reports zero instances.

## Unsubmitted Frame-Retry Candidate At `0b64eabf`

Commit `0b64eabf194d4a58089be358025ace2171767c4b` separates the physical attempt
identity from the participant-local execution frame. `RollbackUnsubmitted` is accepted
only for a pristine Step and now atomically restores every participant's unexecuted
`ExecutionFrameId`. A retry receives a fresh `BatchStepId`, while abort, Drop, live
participant work, and non-pristine states remain non-rewindable and fail closed.

The focused local artifact is:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-frame-retry-local-20260718T085150+0800
```

Its tested diff SHA256 exactly matches `git diff ff917377 0b64eabf`:
`a6f112a5a4ec231ea21f2e5c8934c63a4c1480b3f9b7d7c8631aea01e1e5d6db`.
The artifact includes interfaces `160/160`, submission-wave `3/3`, operation-wave
`8/8`, operation-batch `2/2`, event-journal, scheduler/engine partial-prefill, five-crate
compile, and formatting evidence. Its operation identity contract proves that a retry
after an unsubmitted rollback still emits first physical identity
`frame=1 / sequence=5 / node_invocation=1`. The exact local line is:

```text
FERRUM RUNTIME VNEXT FRAME RETRY LOCAL PASS: /Users/chejinxuan/ferrum-artifacts/runtime-vnext-frame-retry-local-20260718T085150+0800
```

This remains local prediction evidence only. The next clean CUDA run must pass the same
backing-maintenance probe, emit the first physical journal frame contiguously, produce
positive content and usage with exactly one `[DONE]`, and finish with the official S1
decode-capacity PASS line. Any new explicit failure is a REJECT and stops the paid lane.

## Reused-State CUDA REJECT At `0b64eabf`

The clean one-RTX-4090 follow-up used source SHA
`0b64eabf194d4a58089be358025ace2171767c4b` and release binary SHA256
`8ca51f5c548567481d2e44447231255dda9b50b9b861dbc8e09cb95968f99ce8`.
The complete copied artifact is:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-s1-frame-retry-0b64eabf-20260718/cuda-reject-45205420
```

The maintenance probe and target A/B completed, and the earlier operation-view and
request-journal failures did not recur. Target C was admitted after owner release and
returned HTTP 200, `completion_tokens=16`, one usage row, and exactly one `[DONE]`, but
emitted zero content chunks. The same C prompt emitted 16 content chunks during
calibration and target sizing. The collector therefore rejected the run with:

```text
FERRUM RUNTIME VNEXT S1 CUDA DECODE CAPACITY FAIL: /workspace/artifacts/runtime-vnext-s1-frame-retry-0b64eabf-20260718/raw: C: no content chunks
```

The differential evidence narrowed the failure to first use of reused Sequence state
backing. Dynamic CUDA allocations are uninitialized, but Qwen3.5 recurrent convolution,
delta-rule, and paged-KV state had no semantic initialization contract. A new sequence
could therefore inherit released state bytes from another sequence. The inference is
source- and artifact-backed, but remains a hypothesis until a clean CUDA rerun passes.

Collector exit was `1` and validator exit was `125`; no PASS line was produced.
Instance `45205420` was destroyed after artifact collection, and
`inventory-after-destroy.json` reports zero instances.

## Typed State-Initialization Candidate At `3418e99e+dirty`

The candidate adds `StateInitialization::{None, Zero}` to model state and compiled
dynamic-resource contracts. Qwen3.5 declares every recurrent convolution/delta state
as Sequence-scoped Zero state. Its paged KV state remains provider-defined
because each valid slot is written before it can be read; unused block capacity is not
cleared on the decode path. Each newly acquired physical extent generation owns a fresh
typed initialization authority with `Pending -> Prepared -> InFlight -> Initialized`
and fail-closed `Poisoned` transitions. Exact physical segments are zeroed before model
providers in the same command batch and fence. A definitely-not-submitted retry retains
the prepared authority and repeats the zero commands; later decode frames skip the
atomic initialized fast path. Backing extension initializes only the newly acquired
extent, while extent reuse creates a new Pending authority even at the same address.

Request-scoped Zero state is deliberately rejected until core has a scheduler-visible
initialization dependency that can defer sibling sequences. This avoids either duplicate
initialization or blocking a user request behind an invisible mutex.

The focused local artifact is:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-state-initialization-local-20260718T105334+0800
```

It records formatting, diff hygiene, 195 passing interface/resource/operation/wire/
planner tests, the Qwen3.5 typed-family assertion, and all-target compilation of
`ferrum-interfaces`, `ferrum-models`, `ferrum-engine`, `ferrum-server`, and `ferrum-cli`.
The exact local line is:

```text
FERRUM RUNTIME VNEXT STATE INITIALIZATION LOCAL PASS: /Users/chejinxuan/ferrum-artifacts/runtime-vnext-state-initialization-local-20260718T105334+0800
```

This is local prediction evidence only. The next paid lane is one RTX 4090 and the same
Qwen3.5 S1 decode-capacity correctness smoke, not a performance sweep. The falsifiable
prediction is that target C emits at least one content chunk, positive usage, and exactly
one `[DONE]`; no `<unk>`, `[PAD]`, mojibake, panic, or initialization error is accepted.
Any REJECT stops the instance after artifact collection. Official success still requires:

```text
FERRUM RUNTIME VNEXT S1 CUDA DECODE CAPACITY PASS: <out_dir>
FERRUM GATE vnext-s1-cuda-decode-capacity PASS: <out_dir>
```

## Typed State-Initialization CUDA PASS At `c0d56b43`

The clean correctness lane used source SHA
`c0d56b432af39e9b563a210b8a89dbf1cf10e0be`, release binary SHA256
`f39401a2f9b70af19eae0b4052ed947ff380670fdf2108408a0b959ab3a664aa`,
Qwen3.5-4B, and exactly one RTX 4090. The CUDA build completed in 34 minutes 10
seconds. Instance `45210596` was billable for approximately 43.90 minutes at
`$0.39556/hour`, approximately `$0.2894`, and was destroyed after collection.
The final Vast inventory contains zero instances.

The immutable raw CUDA artifact is:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-state-initialization-c0d56b43-20260718/cuda-result-45210596/remote-artifact/raw
```

`ferrum run` returned `Paris`, two tokens, and `finish_reason=stop`. The target
`ferrum serve` streams completed A/B/C with 128/128/16 content chunks and
128/128/16 completion tokens. Every stream returned HTTP 200, one usage row, one
`[DONE]`, and no error. The capacity trace records three typed device-backing
deferrals, one cohort split, one park, one resume, one peer handoff, and one armed
and completed release fence. All resource pools were quiescent after the workload.

The initial remote validator correctly retained the raw evidence but rejected it
because it required different canonical server policies to share one `plan_hash`,
then mapped device-backing deferrals to direct extension-admission counters. Validator
commit `e95af48b797c229380980dc68c1ceb1ce4b2973a` now requires stable model/family/
program/runtime identity across processes, stable plan identity within each process,
canonical command-policy correspondence, and source-specific counter provenance.
The clean validator worktree consumed the immutable CUDA artifact and produced:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-state-initialization-c0d56b43-20260718/run-gate-clean-e95af48b-20260718T1308
```

```text
FERRUM RUNTIME VNEXT S1 CUDA DECODE CAPACITY PASS: /Users/chejinxuan/ferrum-artifacts/runtime-vnext-state-initialization-c0d56b43-20260718/run-gate-clean-e95af48b-20260718T1308
FERRUM GATE vnext-s1-cuda-decode-capacity PASS: /Users/chejinxuan/ferrum-artifacts/runtime-vnext-state-initialization-c0d56b43-20260718/run-gate-clean-e95af48b-20260718T1308
```

This closes the reused recurrent-state corruption and bounded CUDA decode-capacity
resource blocker. The artifact explicitly does not prove complete S1, G04,
performance, Metal correctness, or release readiness. The full validator suite still
has a pre-existing unrelated failure because the historical-corpus registration for
`G00_BASELINE.md` is stale; the focused decode-capacity self-test and `run_gate`
self-test pass.

## Shared Initial-Capacity CUDA REJECT At `c35982bb`

The later shared-capacity lane used source SHA
`c35982bb522247a03a30dc349986fe21f1838d2a`, release binary SHA256
`edfd556844ad125113e5a3b50f82a29a1bd1292d20668f252956b35e5eeb797a`,
Qwen3.5-4B, and exactly one RTX 4090. Its complete copied artifact is:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-s1-capacity-c35982bb-20260718
```

The lane rejected the required A-admit/B-wait/C-bypass/A-release/B-resume
lifecycle. C bypass was `0/1`; the observed order was A, B, C. The exact final lines
were:

```text
FERRUM RUNTIME VNEXT S1 CUDA CAPACITY PRESSURE FAIL: /workspace/artifacts/runtime-vnext-s1-capacity-c35982bb-20260718/gate: typed capacity lifecycle is not A-admit/B-wait/C-bypass/A-release/B-resume
FERRUM GATE vnext-s1-cuda-capacity FAIL: /workspace/artifacts/runtime-vnext-s1-capacity-c35982bb-20260718/gate: delegated command failed rc=1
```

The artifact classifies the failure as
`partial_initial_admission_cross_pool_head_of_line_block`. Initial request and first
sequence admission were separate transactions. B retained request resources after its
sequence fit deferred; C then alternated request/recurrent/paged-KV reclamation until B
completed. This is a production scheduling/resource defect, not a validator failure.
No performance claim is valid from this run.

Instance `45218085` was stopped after artifact collection. The retained instance has
useful model/build caches; `inventory-after-reject.json` records
`cur_state=stopped`, `actual_status=exited`, and zero potentially billable siblings.

## Atomic Initial-Admission Candidate

The source candidate replaces the product's two-stage initial request/sequence path
with one typed `InitialSequenceBundle` transaction:

- request and sequence logical capacity are committed atomically under one mutation
  lock;
- mixed-scope physical claims are prepared as one transaction and roll back in full
  on any defer or rejection;
- a physical deferral owns no partial request or sequence lease;
- cross-pool maintenance protects the exact immediate bundle demand, rather than only
  per-pool resident/used floors;
- retry state freezes the typed work shape and no longer uses retained VRAM authority
  as a retry token.

Bounded local prediction checks on the dirty candidate passed:

```text
CARGO_BUILD_JOBS=4 RUST_TEST_THREADS=2 cargo test -p ferrum-interfaces --lib -- --test-threads=2
test result: ok. 166 passed; 0 failed

CARGO_BUILD_JOBS=4 RUST_TEST_THREADS=2 cargo test -p ferrum-models --lib vnext_executor::tests -- --test-threads=2
test result: ok. 4 passed; 0 failed

CARGO_BUILD_JOBS=4 cargo check -p ferrum-models -p ferrum-engine -p ferrum-server -p ferrum-cli --all-targets
Finished `dev` profile successfully

python3 scripts/release/runtime_vnext_s1_cuda_capacity.py --self-test
FERRUM RUNTIME VNEXT S1 CUDA CAPACITY SELFTEST PASS
```

The focused contract proves A admitted, B deferred with zero partial request/sequence
claims, C admitted while A remained active, and all claims released without threads.
This is source-level prediction only. It does not replace real CUDA evidence and does
not close G04.

The next paid action is exactly one reuse of stopped instance `45218085` for the
Qwen3.5-4B `vnext-s1-cuda-capacity` correctness lane. The required signal is B waiting
with `InitialSequenceBundle` evidence, C completing before A releases, no alternating
recurrent/paged-KV reclaim loop, and B resuming after A release. The stop condition is
the first exact PASS or REJECT; artifacts are copied back and the instance is stopped
in either case. Success requires both exact lines:

```text
FERRUM RUNTIME VNEXT S1 CUDA CAPACITY PRESSURE PASS: <out_dir>
FERRUM GATE vnext-s1-cuda-capacity PASS: <out_dir>
```

## Atomic Initial-Admission CUDA PASS At `1903a594`

The clean follow-up used source SHA
`1903a5943392565d70ecd8abbe7e6f2814157519`, release binary SHA256
`dd02d6005be7f9b87bb50f082621b8454312943207fab5fed16487c76565cc6d`,
Qwen3.5-4B, and exactly one RTX 4090. The complete copied artifact is:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-s1-capacity-1903a594-20260718
```

The local validator consumed the copied immutable raw artifact and printed:

```text
FERRUM RUNTIME VNEXT S1 CUDA CAPACITY PRESSURE PASS: /Users/chejinxuan/ferrum-artifacts/runtime-vnext-s1-capacity-1903a594-20260718/gate-local
FERRUM GATE vnext-s1-cuda-capacity PASS: /Users/chejinxuan/ferrum-artifacts/runtime-vnext-s1-capacity-1903a594-20260718/gate-local
```

The measured lifecycle closed the `c35982bb` failure:

- B deferred before physical preparation on the atomic bundle's domain `4`
  `fit_availability`: requested `8,388,608`, available `6,815,744`, action
  `WaitForRelease`, and `prefill_submit_observed=false`;
- B's wait snapshot retained only A: `active_requests=1`, `active_sequences=1`, while
  `staged_prefill_requests=0` and `staged_prefill_sequences=0`;
- C finished `10.106312283` seconds before A finished; B emitted its first content
  `0.113531965` seconds after A finished, proving A/C/B rather than the rejected A/B/C
  order;
- A/B/C produced `224/32/16` content chunks and completion tokens respectively; all
  returned HTTP 200 with one usage row, exactly one `[DONE]`, and no error;
- lifecycle duration was `13.080717110` seconds, unchanged-source skips were `218/512`,
  and the pressure trace was `3,448,924` bytes, below the 16 MiB ceiling;
- terminal state had zero active sequences, pending sequences, staged prefills,
  executing prefills, pending prefill maintenance, live backing segments, failed waves,
  quarantine, and poisoned pools.

Because the coherent logical preflight deferred B before physical preparation, this
CUDA workload did not itself force B through a physical `InitialSequenceBundle`
deferral. The mixed-scope physical rollback and exact cross-pool protection remain
covered by the bounded local contract. The CUDA artifact proves the required product
behavior and absence of partial initial ownership, not every physical maintenance
branch in isolation.

The retained cached instance `45218085` was billable for approximately `857` seconds
at `$0.39556/hour`, approximately `$0.0942`. It was stopped after artifact copy-back;
`inventory-after-pass.json` records `cur_state=stopped`, `actual_status=exited`, and no
potentially billable sibling. The CUDA build took approximately 7 minutes 5 seconds.

This closes the Qwen3.5-4B shared initial-capacity S1 lane at this SHA. It does not by
itself prove complete S1, aggregate G04, performance, Metal correctness, or release
readiness.

## Canonical Basic Collection REJECT And Terminal-Usage Fix

The canonical collector run at clean source SHA
`594c217b58df17d3d375abaf16ce40fc8bafb062` completed all eight fixed
ABBA-BAAB slots on retained instance `45218085`. Its complete copied artifact is:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-s1-basic-594c217b-20260718
```

The correctness checkpoint passed before performance collection, but the formal S1
validator correctly rejected `basic1` because every measured request reported `66`
terminal output frames for at most `64` usage tokens:

```text
FERRUM RUNTIME VNEXT S1 CUDA BASIC SLICE FAIL: profile-overhead/basic1: terminal output count is outside 1..64
FERRUM GATE vnext-s1-cuda FAIL: delegated command failed rc=1
```

This exposed a production lifecycle boundary defect. Generic `release_cache` both
released physical authority and asserted successful request completion, while the
vNext journal used completed physical execution frames as output-token evidence.
Commit `51b71f5bbc7bd6baab79d5509c7c82543a895f7a` introduces an
`ExecutorSequenceCompletion` receipt carrying request/cache identity and the engine's
final user-visible input/output usage. Normal completion consumes that receipt;
cancel, failure, and recompute retain release/abort semantics. The journal no longer
infers token counts from prefill/decode frame topology.

Bounded local validation passed:

```text
CARGO_BUILD_JOBS=4 cargo check -p ferrum-interfaces -p ferrum-engine -p ferrum-models --all-targets
CARGO_BUILD_JOBS=4 RUST_TEST_THREADS=2 cargo test -p ferrum-engine plan_runtime_product_path_requires_typed_admission_before_prefill -- --test-threads=2
test result: ok. 1 passed; 0 failed
```

The targeted CUDA diagnostic reused the same cached RTX 4090 and collected exactly
15 requests rather than repeating the eight performance slots. The copied artifact is:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-s1-terminal-51b71f5b-20260718
```

It validates 5,985 typed events, one captured frame per request, zero operation
identity omissions, `897,484.53` trace bytes/request, and exact terminal/usage
reconciliation for all 15 requests. The terminal counts are
`42/64/64/53/64/64/64/64/64/64/64/64/64/64/64`; no `66` remains. Its exact
diagnostic line is:

```text
FERRUM RUNTIME VNEXT S1 TERMINAL USAGE DIAGNOSTIC PASS: /workspace/ferrum-artifacts/runtime-vnext-s1-terminal-51b71f5b-20260718/basic-fix
```

The release CUDA relink took 6 minutes 58 seconds despite the five-file scoped
change. The paid session was approximately 54.67 minutes at `$0.39556/hour`, or
approximately `$0.3604`. After artifact copy-back, instance `45218085` was stopped;
the final inventory records `cur_state=stopped`, `actual_status=exited`, and no
potentially billable sibling.

This diagnostic closes only `terminal_usage_counter_drift`. It does not turn the
original performance artifact into a PASS and does not prove S1, G01B, G04, Metal,
or release readiness.

## Basic-Slice Variance Classification

Across all eight original slots, `off` measured `16.3904` mean tokens/s with
`14.09%` CV, while `basic` measured `17.9276` mean tokens/s with `10.53%` CV.
Mean and median overhead were negative (`-9.38%` and `-10.76%`), so the current
blocker is not evidence of basic-sink overhead. The `off` slots alone alternate
between a fast regime (`18.65`, `18.31`) and slow regime (`14.16`, `14.43`). Slow
slots have proportionally lower GPU utilization and power, while P-state, SM clocks,
temperature, scheduler event counts, and host busy percentage remain comparable.

The durable diagnostic hypothesis is:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-s1-basic-594c217b-20260718/performance-hypothesis.json
```

The host has two CPU sockets, the RTX 4090 is local to NUMA node 1, the container
allows CPUs from both nodes, and the product process was unbound. The next paid
diagnostic, when capacity is deliberately resumed, is a four-slot
node0/node1/node1/node0 causal comparison on the same clean binary. It is diagnostic
only: an external `numactl` wrapper cannot establish product-default performance.
The NUMA hypothesis is rejected if node-1 mean is below `17.5` tokens/s, node-1 CV
exceeds `5%`, or node-0/node-1 differ by less than `5%`. No new paid run should start
without this exact prediction and stop condition.

## NUMA And Host-Handoff Hypotheses REJECT At `f54475fe`

The bounded NUMA comparison at source SHA `51b71f5b` is preserved at:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-s1-numa-causal-51b71f5b-20260718
```

It rejected cross-NUMA placement as the primary cause. GPU-local node 1 averaged
`18.0987 tok/s` with `6.924%` CV, while node 0 averaged `18.4712 tok/s` with
`9.638%` CV. Node 1 was `2.016%` slower rather than at least `5%` faster, and
slot drift crossed the NUMA boundary. No product CPU-affinity default was changed.

Two source changes then removed concrete host-side amplification without changing
backend semantics or product defaults:

- `92ec5732` (`perf(vnext): batch typed execution trace writes`) reduced an actual
  approximately 399 event sink calls per request to approximately five ordered batch
  calls. The product `profile=off` path remains unattached and allocation-free.
- `f54475fe` (`perf(vnext): bound device completion waits`) replaced per-wave/token
  Tokio blocking tasks with one executor-owned OS worker and a queue of capacity one.
  Cancellation cannot orphan a queued or running fence, and quiescent health reports
  one worker, capacity one, zero pending work, and zero panics.

The clean-SHA CUDA collection used Qwen3.5-4B, the official CUDA feature set, and one
RTX 4090. Its binary SHA256 was
`6f539003fdf49639b60e26c27996c2fcb445ed5df758c9c1948e7bb2857b1cf7`.
The complete verified local artifact is:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-s1-basic-f54475fe-20260718
```

Correctness passed before performance collection. Actual `ferrum run` and
`ferrum serve` both produced `Paris`; run/serve emitted 399/2,394 typed events,
stream and usage reconciliation passed, and request failures were zero:

```text
FERRUM RUNTIME VNEXT S1 CUDA TRACE CHECKPOINT PASS: /workspace/ferrum-artifacts/runtime-vnext-s1-basic-f54475fe-20260718/raw
```

All eight ABBA-BAAB performance slots completed, but the formal gate rejected the
evidence and the same result was reproduced from the copied artifact:

```text
FERRUM RUNTIME VNEXT S1 CUDA BASIC SLICE FAIL: profile off CV 0.086373 exceeds 0.05
FERRUM GATE vnext-s1-cuda FAIL: delegated command failed rc=1
```

| Metric | Observed | Required | Result |
|---|---:|---:|---|
| profile-off mean | `17.3409 tok/s` | diagnostic only | low absolute throughput |
| profile-off CV | `8.637%` | `<=5%` | REJECT |
| profile-basic mean | `17.6637 tok/s` | diagnostic only | low absolute throughput |
| profile-basic CV | `10.571%` | `<=5%` | REJECT |
| mean basic overhead | `-1.861%` | `<=2%` | within limit, not accepted under noisy evidence |
| median basic overhead | `-4.854%` | `<=2%` | within limit, not accepted under noisy evidence |

The source hypothesis is rejected: eliminating per-event sink calls and unbounded
blocking-task creation did not bring either group below `5%` CV. Basic profiling was
not slower than off, so the current evidence does not identify trace overhead as the
cause. A fast basic slot measured `19.3801 tok/s`, `30.56%` GPU utilization, and
`108.40W`; a slow basic slot measured `15.6305 tok/s`, `25.92%`, and `97.96W`.
Clocks and temperature did not show thermal throttling. Runtime health selected
`legacy_paged_decode` and `graph_disabled`, and a captured frame contains 131
operations. The existing trace does not separate host encode/submit, completion-queue
delay, device-fence wait, readback/logit conversion, and retirement time.

The next source stop condition is therefore bounded typed phase timing for those five
boundaries. No further paid run is authorized until that local instrumentation yields
a falsifiable prediction for the dominant phase and the expected artifact counter or
trace-shape change. The durable verdict is
`control/final-verdict.json` under the artifact root. The recovered archive SHA256 is
`8d3f09543151b2811e708fa98fcba6d31548be67a01be5e81bc0762d7c17312c`; 246 files and
all eight slots were verified with zero mismatch.

Instance `45218085` is retained only for its model/build cache and was verified
`cur_state=stopped`, `actual_status=exited`; there are no running siblings. This
checkpoint is a correctness PASS and performance REJECT. It does not prove S1,
G01B, performance readiness, Metal correctness, or release readiness.

## Exact Completion Timing REJECT At `ea912783`

Commit `4a319dac` first removed repeated active-binding construction from the wave
hot path. Commit `ea9127836453d8b2c3763984da6be8d9e746ec60` then introduced an
exact-submission timing contract: profile mode is copied into the physical command
batch, device timing can only leave the backend with the quiescent terminal receipt,
and fence wait/readback remain separate host-clock measurements. Profile `off`
creates no CUDA start event or host clock read. Both commits were pushed before paid
validation.

Bounded local validation passed:

```text
CARGO_BUILD_JOBS=4 cargo check -p ferrum-interfaces -p ferrum-models -p ferrum-engine --all-targets
CARGO_BUILD_JOBS=4 RUST_TEST_THREADS=2 cargo test -p ferrum-interfaces --test vnext_device_operation_wave_contract_tests --test vnext_device_operation_completion_contract_tests -- --test-threads=2
CARGO_BUILD_JOBS=4 RUST_TEST_THREADS=2 cargo test -p ferrum-models --lib executor::vnext_executor::tests -- --test-threads=2
CARGO_BUILD_JOBS=4 cargo check -p ferrum-kernels --features metal
```

The retained 1x RTX 4090 instance `45218085` was reconciled as the only instance and
reused with its existing Qwen3.5-4B and CUDA build caches. The CUDA 12.4 release build
completed in `7m03s`; no local binary was uploaded. Actual `ferrum run`, `ferrum
serve`, streaming usage, and the bounded server benchmark passed, and the canonical
validator printed:

```text
FERRUM RUNTIME VNEXT S1 CUDA TRACE CHECKPOINT PASS: /workspace/ferrum-artifacts/runtime-vnext-device-timing-ea912783-20260719/raw
```

The copied diagnostic artifact is:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-device-timing-ea912783-20260719
```

The c=1, input 128/output 64, three-repeat diagnostic completed without request errors
and measured `17.0988 tok/s` mean. It produced `957` completed waves, `957` host fence
wait samples averaging `11.1486ms`, and `957` readback samples averaging `0.4082ms`.
However, device elapsed time had `0` samples and `957` typed unavailable rows, so the
timing stop condition rejected the run. The host timing remained diagnostic-only:
completion task run averaged `28.3706ms`, while host encode/submit averaged
`16.9840ms`; neither number proves device execution time.

Source inspection identified the exact failure class. In cudarc 0.19.4,
`record_event(None)` creates `CU_EVENT_DISABLE_TIMING`; both the start event and the
existing terminal fence event therefore rejected `cuEventElapsedTime`. Commit
`b4c6f64e` changes only profiled start/terminal events to `CU_EVENT_DEFAULT`; the off
path retains timing-disabled events. The bounded follow-up prediction is nonzero
device samples and zero device-unavailable rows. No third paid diagnostic is allowed
if that prediction fails.

## Exact Completion Timing KEEP At `b4c6f64e`

The single permitted follow-up reused instance `45218085` and its existing build and
Qwen3.5-4B caches. The clean CUDA release binary was rebuilt from
`b4c6f64efe7bd0d3fabc5a3488694b05b919228f` in `6m48s`; its SHA256 is
`31f284b3bb13c5dc90d3838659b8a9db40266242a73f35848585e525cc51b4cd`.
Actual `ferrum run`, `ferrum serve`, streaming usage, and the bounded server benchmark
were recollected at that SHA. Both the remote validator and the validator rerun after
copying the evidence to macOS printed:

```text
FERRUM RUNTIME VNEXT S1 CUDA TRACE CHECKPOINT PASS: /Users/chejinxuan/ferrum-artifacts/runtime-vnext-device-timing-b4c6f64e-20260719/raw
```

The one-repeat c=1 timing probe used two measured random requests at input 128/output
16 with `--fail-on-error --seed 9271`. It completed `2/2` requests with zero errors and
measured `16.3127 tok/s`. Across the resulting `54` physical submissions, all `54`
device event elapsed measurements were available and the typed unavailable count was
zero. The bounded averages were:

```text
device stream elapsed: 21.0906ms
host fence wait:       11.6149ms
host readback:          0.3054ms
completion task run:   26.9405ms
host encode/submit:    20.2028ms
```

This confirms the exact-completion attribution path and classifies the implementation
as KEEP. It does not turn the small probe into a performance result: device event
elapsed and host intervals use different clocks and may overlap, so they must not be
added. The current evidence instead narrows the next source investigation to two
independent regions: the roughly `20ms` host encode/submit boundary and the completion
worker work outside fence wait/readback. Any next timing boundary must separate typed
completion finalization/resource retirement from the already measured fence and
readback intervals rather than infer it by subtraction.

The copied artifact and diagnostic classification are:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-device-timing-b4c6f64e-20260719/raw
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-device-timing-b4c6f64e-20260719/control/device-timing-keep.json
```

Measured build-through-probe time was `735.3s`, approximately `$0.0808` at
`$0.39556/hour`. After the artifacts were copied, instance `45218085` was stopped and
verified at `cur_state=stopped`, `actual_status=exited`; inventory contained no other
potentially billable instance. This is a correctness checkpoint plus a diagnostic
KEEP. It does not prove formal S1, G01B, Metal correctness, release performance, or
release readiness.

## Batch Readback Fingerprint KEEP At `ecac974c`

Source attribution of the remaining completion interval found a contract inconsistency.
`CompletionReadbackOutput` already SHA256-hashed the exact `496,640B` logits readback,
and the single-readback receipt correctly fingerprinted the request, typed disposition,
and output digest. The batch receipt instead serialized the complete disposition,
including raw logits bytes as a JSON integer array, and SHA256-hashed that temporary
payload a second time on every physical wave. Commit
`ecac974c4e91da25fd3028786971416714426c9e` replaces that path with a bounded
metadata projection over request, typed status/failure, and the existing output digest.
The fingerprint domain advances from batch readback v1 to v2; the receipt still owns
the exact raw output bytes.

Bounded local validation passed:

```text
CARGO_BUILD_JOBS=4 RUST_TEST_THREADS=2 cargo test -p ferrum-interfaces --lib fingerprint_tests::batch_fingerprint_is_bounded_by_digest_evidence_not_output_bytes -- --test-threads=2
CARGO_BUILD_JOBS=4 RUST_TEST_THREADS=2 cargo test -p ferrum-interfaces --test vnext_device_operation_batch_contract_tests --test vnext_device_operation_completion_contract_tests -- --test-threads=2
CARGO_BUILD_JOBS=4 cargo check -p ferrum-interfaces -p ferrum-models --all-targets
```

The new 512 KiB contract test proves that batch fingerprint input remains below 1 KiB,
is deterministic, changes with the output digest, and distinguishes success from
not-attempted. The first compile check failed only because the local filesystem had
`49MiB` free; deleting `5GiB` of generated `target/debug/incremental` state restored the
build, and the identical check then passed.

The paid follow-up reused the single retained RTX 4090 instance and caches. The clean
CUDA release build took `422.6s`; binary SHA256 was
`40f0186e170936a1269e585227c773767b0df076a6067f70be0f20b34cf15e6d`.
Actual `ferrum run`, `ferrum serve`, streaming usage, and the bounded server benchmark
passed before timing was interpreted. Both the remote and copied-artifact validators
printed:

```text
FERRUM RUNTIME VNEXT S1 CUDA TRACE CHECKPOINT PASS: /Users/chejinxuan/ferrum-artifacts/runtime-vnext-batch-fingerprint-ecac974c-20260719/raw
```

The identical c=1, input 128/output 16, two-request diagnostic confirmed every planned
stop condition:

| Metric | `b4c6f64e` baseline | `ecac974c` | Change |
|---|---:|---:|---:|
| completion worker task run | `26.9405ms` | `17.2230ms` | `-36.070%` |
| completion round trip | `27.0123ms` | `17.2833ms` | `-36.017%` |
| submitted wave total | `51.1564ms` | `37.7332ms` | `-26.240%` |
| device stream elapsed | `21.0906ms` | `20.5488ms` | `-2.569%` |
| output throughput | `16.3127 tok/s` | `21.8499 tok/s` | `+33.944%` |

All `54` candidate physical submissions produced device elapsed evidence with zero
typed unavailable rows, and the two measured requests completed without errors or
quality flags. The nearly unchanged device elapsed interval, alongside the `9.72ms`
completion reduction, confirms that the gain came from host receipt processing rather
than a GPU timing shift. The same-host residual after measured fence wait and readback
fell from `15.0202ms` to `5.0611ms`; this remaining interval is the bounded target for
receipt/resource retirement analysis, not evidence for another speculative GPU run.

The copied artifact and KEEP classification are:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-batch-fingerprint-ecac974c-20260719/raw
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-batch-fingerprint-ecac974c-20260719/control/batch-fingerprint-keep.json
```

Vast reported `14.2004` minutes uptime, approximately `$0.0936`. After copying all 75
artifact files, instance `45218085` was verified at `cur_state=stopped`,
`actual_status=exited`; no potentially billable sibling remained. The `+33.944%`
throughput delta is diagnostic-only because the probe has one repeat and two measured
requests. This checkpoint does not prove formal S1, G01B, Metal correctness, release
performance, or release readiness.

## Shared Submission Evidence KEEP At `97a1ff01`

Profile-attached-only dispatch timing at `75ba7d88` split the old host encode/submit
interval without adding a clock read to `profile=off`. Source attribution then found
that immutable execution and submission evidence was recursively copied across wave,
completion and event ownership boundaries. Batch identity construction also allocated
canonical JSON for the full nested node/operation/participant tree even though every
node already had a validated fingerprint.

Commit `97a1ff01023ea16a5eec6382c81b4bc12402c823` makes execution envelopes,
batch/participant identities and submitted receipts cheap immutable shared values while
preserving their exact serialized wire shapes. Fingerprints stream serde output directly
into SHA256. Batch identity advances to v2 and composes ordered validated node digests
instead of reserializing the nested identity tree. A real 32-participant dispatch test
proves that the public evidence handles are pointer-sized, their JSON has no private
wrapper, clone serialization is identical, and submit/readback ownership still closes.

Bounded local validation passed with `CARGO_BUILD_JOBS=4` and
`RUST_TEST_THREADS=2`: the fingerprint equivalence/projection tests, real
32-participant dispatch, completion contracts, nine wave-dispatch contracts, event
execution/replay contracts, and an all-target check across `ferrum-interfaces`,
`ferrum-models`, and `ferrum-kernels`.

The paid follow-up reused the only retained instance, RTX 4090 instance `45218085`, and
its source/build/model caches. Source was fetched from GitHub at the exact clean SHA.
The CUDA release build command was:

```text
CARGO_BUILD_JOBS=8 cargo build --release -p ferrum-cli --bin ferrum --features cuda,vllm-moe-marlin,vllm-paged-attn-v2
```

The build took approximately `7m00.3s`; binary SHA256 was
`89d621cc29fa93e34ba0e4717f155fdc68d1b38fc828c361e8449a4c7069d2c1`.
Actual `ferrum run`, `ferrum serve`, streaming usage and the bounded server benchmark
passed before timing was interpreted. The final remote validator and the validator
rerun after copying all 79 artifact files to macOS printed:

```text
FERRUM RUNTIME VNEXT S1 CUDA TRACE CHECKPOINT PASS: /Users/chejinxuan/ferrum-artifacts/runtime-vnext-shared-evidence-97a1ff01-20260719/raw
```

The identical c=1, input 128/output 16 probe completed both measured requests with zero
errors. All `54` physical submissions had exact device timing and zero unavailable rows:

| Metric | `ecac974c` baseline | `97a1ff01` | Change |
|---|---:|---:|---:|
| host encode/submit | `17.9077ms` | `13.8794ms` | `-22.495%` |
| completion worker task run | `17.2230ms` | `15.0224ms` | `-12.777%` |
| completion host residual | `5.0611ms` | `2.3595ms` | `-53.380%` |
| submitted wave total | `37.7332ms` | `30.8141ms` | `-18.337%` |
| device stream elapsed | `20.5488ms` | `20.6348ms` | `+0.418%` |
| output throughput | `21.8499 tok/s` | `26.9928 tok/s` | `+23.538%` |

The new host interval breakdown is decisive:

| Profile-attached substage | Average | Samples |
|---|---:|---:|
| token upload preparation | `0.0010ms` | `54` |
| wave identity binding | `1.5281ms` | `54` |
| provider encode and submit | `12.3488ms` | `54` |

The nearly unchanged device interval and the reduced host/completion intervals confirm
that repeated immutable evidence processing was a real host bottleneck. The remaining
host cost is now bounded to `provider_encode_submit`, which still combines completion
reservation, backing/input encoding, provider node encoding and lane reservation/submit.
The next source change must split those four typed boundaries and optimize only the
measured dominant substage; another paid sweep before that attribution is prohibited.

The copied artifact and KEEP classification are:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-shared-evidence-97a1ff01-20260719/raw
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-shared-evidence-97a1ff01-20260719/control/shared-evidence-keep.json
```

The confirmed-running to confirmed-exited window was `1351s`, approximately `$0.1484`
at `$0.39556/hour`; provider transition billing may differ. Instance `45218085` is
verified at `cur_state=stopped`, `actual_status=exited`, with no potentially billable
sibling. One shell quoting failure in the timing orchestrator was retained as a
script-only retry artifact; product correctness passed before it and passed again after
the successful bounded probe. The throughput delta remains diagnostic-only because it
uses one repeat and two requests. This checkpoint does not close formal S1 A/B
stability, G01B, Metal correctness, release performance, or release readiness.

## Prepared Dispatch Binding REJECT At `080a7e41`

Commit `080a7e4106121737706b011630697279774b5131` moves immutable operation
catalog, provider, resource and plan-slot proofs to `bind_plan`. The submitted hot path
keeps generation, authority, backing, allocation and runtime-descriptor checks, but no
longer rebuilds the static binding maps and sets for every physical wave. Focused wave
and dispatch contracts passed with `RUST_TEST_THREADS=2`, and all-target checks passed
for `ferrum-interfaces` and `ferrum-models`.

The bounded CUDA follow-up reused the only retained RTX 4090 instance and its caches.
The clean release build took approximately `7m07s`; binary SHA256 was
`a1493a06c779fd75d5f6f37a348d492bdb341053df5c5a3def6ab11720112346`.
Actual `ferrum run`, `ferrum serve`, and streaming usage passed before timing was
interpreted. The copied-artifact validator printed:

```text
FERRUM RUNTIME VNEXT S1 CUDA TRACE CHECKPOINT PASS: /Users/chejinxuan/ferrum-artifacts/runtime-vnext-prepared-dispatch-080a7e41-20260719/raw
```

The identical c=1, input 128/output 16 probe completed two measured requests with zero
errors and produced timing for all `54` physical waves. The predeclared performance
hypothesis was rejected:

| Metric | `97a1ff01` baseline | `080a7e41` | Change | Required |
|---|---:|---:|---:|---:|
| host encode/submit | `13.8794ms` | `13.9242ms` | `+0.323%` | `<=12.5000ms` |
| provider encode/submit | `12.3488ms` | `11.6646ms` | `-5.541%` | `<=11.1000ms` |
| device stream elapsed | `20.6348ms` | `20.2468ms` | `-1.880%` | within `5%` |

The provider boundary now attributes its average time as follows:

| Profile-attached substage | Average | Share | Maximum |
|---|---:|---:|---:|
| contract validation and completion reservation | `0.1171ms` | `1.00%` | `0.3353ms` |
| backing and input encode | `0.0089ms` | `0.08%` | `0.0498ms` |
| provider node encode | `2.0273ms` | `17.38%` | `2.8010ms` |
| lane reserve, runtime submit and completion arm | `9.5107ms` | `81.53%` | `206.2548ms` |

The static-proof change remains an architectural cleanup, and its aggregate provider
segment improved by `5.54%`, but it did not reduce total host submission time. It is
therefore not a performance KEEP. Source attribution shows that the dominant boundary
contains lane reservation, CUDA context/event setup, serial enqueue of the `131` plan
commands, fence creation and completion arming. The next source step must split lane
reservation, `DeviceRuntime::submit`, and completion arming into independent typed
metrics before assigning the `9.51ms` to any one mechanism or paying for another run.

The copied artifact and REJECT classification are:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-prepared-dispatch-080a7e41-20260719/raw
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-prepared-dispatch-080a7e41-20260719/control/prepared-dispatch-reject.json
```

The confirmed-running to confirmed-exited window was `1044s`, approximately `$0.1147`
at `$0.39556/hour`. Instance `45218085` is verified at `cur_state=stopped`,
`actual_status=exited`, with no potentially billable sibling. The observed
`23.4436 tok/s` is diagnostic-only and is not a performance claim because the probe has
one repeat and two measured requests. This checkpoint does not close formal S1, G01B,
Metal correctness, release performance, or release readiness.

## CUDA Submit Boundary Attribution KEEP At `2d240455`

Commit `2d240455ed60e0ffcf1f5f2ac812f74c1d897e91` retains the comparable
`lane_reserve_submit_arm` aggregate and adds profile-attached-only typed intervals for
lane acquisition, `DeviceRuntime::submit`, and successful completion arming. The
successful-path contract proves the exact event order, nine wave contracts passed with
`RUST_TEST_THREADS=2`, the metrics schema test passed, and all-target checks passed for
`ferrum-interfaces` and `ferrum-models`.

The bounded CUDA follow-up reused the only retained RTX 4090 instance and its source,
build, and model caches. The release build took `7m04s`; binary SHA256 was
`9dc71cd9f7c601d71214e489fb828fd1a01b2760d29a2fb16f19877372d48706`.
A first controller attempt exited `127` before Rust compilation because its detached
shell did not load `/root/.cargo/env`; that attempt is preserved as a script-only retry.
Actual `ferrum run`, `ferrum serve`, and streaming usage passed before timing was
interpreted. The validator rerun against the copied local artifact printed:

```text
FERRUM RUNTIME VNEXT S1 CUDA TRACE CHECKPOINT PASS: /Users/chejinxuan/ferrum-artifacts/runtime-vnext-submit-boundary-2d240455-20260719/raw
```

The previous aggregate maximum exceeded `206ms`, so this diagnostic isolated one
warmup request and saved a health snapshot before running two measured requests. The
warmup contained a `220.1683ms` `DeviceRuntime::submit` maximum. Counter differences
between the post-warmup and post-measurement snapshots therefore provide `36`
steady-state physical waves without attributing first-request CUDA lazy initialization
to steady-state submission:

| Steady-state interval | Average per wave | Share of lane aggregate |
|---|---:|---:|
| lane reserve, submit and arm | `4.1964ms` | `100.000%` |
| lane acquisition | `0.0009ms` | `0.021%` |
| `DeviceRuntime::submit` | `4.1585ms` | `99.096%` |
| completion arm | `0.0367ms` | `0.875%` |

The three nested intervals cover `99.993%` of the aggregate. Both measured requests
completed with zero errors or quality flags, usage-counted output, and diagnostic
throughput of `27.6435 tok/s`. Other steady-state boundaries were:

| Interval | Average per wave |
|---|---:|
| total host encode/submit | `7.2466ms` |
| wave identity bind | `1.5037ms` |
| provider node encode | `1.4402ms` |
| exact device-event execution | `16.9623ms` |
| completion round trip | `15.5379ms` |
| submitted wave total | `24.6130ms` |

This is an attribution KEEP, not a performance KEEP. It rules out the execution-lane
mutex and completion arming as the material source of the measured submission cost.
The remaining host bottleneck is inside `CudaDeviceRuntime::submit`, which still
combines context binding, timing-event setup, serial enqueue of the full command batch,
fence recording, and stream accounting. The Qwen3.5-4B plan has `131` provider nodes
per physical wave, plus its input command. The next source step must split those
backend-internal intervals before choosing between compiled command replay, CUDA Graph,
or a narrower event/context fix. The `16.96ms` device interval also remains larger than
host runtime submission and may contain launch-feed gaps; it is not valid to claim that
removing host submission alone closes the performance gap.

The copied artifact and machine-readable attribution are:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-submit-boundary-2d240455-20260719/raw
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-submit-boundary-2d240455-20260719/control/submit-boundary-keep.json
```

Vast reported `18.0609` minutes uptime, approximately `$0.1191` at `$0.39556/hour`.
Instance `45218085` is verified at `cur_state=stopped`, `actual_status=exited`, and the
inventory contains no potentially billable sibling. The two-request one-repeat result
is diagnostic-only and proves no formal S1, G01B, Metal correctness, release
performance, or release readiness.

## CUDA Submit Internal Attribution KEEP At `2799db81`

Commit `2799db812fc8e47646a14dd72bedec02c3a40353` adds a generic typed
`DeviceSubmissionTimingSink` and splits the CUDA runtime submission interval into
validation/preparation, start-event recording, ordered command enqueue, and fence
recording/accounting. The disabled specialization does not read a clock. Existing
backends retain the original submission contract through the default implementation.
Bounded local validation passed: all-target checks for `ferrum-interfaces`,
`ferrum-kernels`, and `ferrum-models`, all nine prepared-wave contracts, and the
executor timing-schema test. Local Rust execution remained bounded to
`CARGO_BUILD_JOBS=4` and `RUST_TEST_THREADS=2`.

The bounded CUDA follow-up reused the only retained RTX 4090 instance and fetched the
exact clean commit through GitHub. The release build took `7m03s`; binary SHA256 was
`bc0fb09b671c0802a9d1ada33548ae35b34c99384d6c746c31b33dac04c322c2`. Actual
`ferrum run`, `ferrum serve`, streaming usage, and the server smoke passed before the
timing result was interpreted. The validator rerun against the copied local artifact
printed:

```text
FERRUM RUNTIME VNEXT S1 CUDA TRACE CHECKPOINT PASS: /Users/chejinxuan/ferrum-artifacts/runtime-vnext-submit-internal-2799db81-20260719/raw
```

One isolated warmup request was excluded from the steady-state counter differences.
The two measured c=1 requests produced exactly `36` physical waves, completed with zero
errors and zero output-quality flags, and used server-reported usage token counts. The
four internal intervals cover `99.986%` of `CudaDeviceRuntime::submit`:

| Steady-state CUDA submit interval | Average per wave | Share of runtime submit |
|---|---:|---:|
| validate, bind context, and prepare | `0.0017ms` | `0.026%` |
| record device timing start | `0.0160ms` | `0.241%` |
| enqueue the ordered command batch | `6.6250ms` | `99.602%` |
| record fence and account stream state | `0.0078ms` | `0.117%` |
| total `DeviceRuntime::submit` | `6.6515ms` | `100.000%` |

The enclosing lane interval averaged `6.7250ms`; runtime submit represented `98.906%`,
lane acquisition averaged `0.0008ms`, and completion arming averaged `0.0722ms`.
The exact device-event interval remained `16.8261ms` per wave. The absolute diagnostic
throughput was `22.4815 tok/s`, but this one-repeat, two-request number is not a
performance comparison or release claim.

This is a decisive attribution KEEP. Context binding, event setup, fence accounting,
and the execution-lane mutex are not the material host bottleneck. The current eager
CUDA path constructs one-shot boxed commands for each physical wave and dynamically
invokes one input command plus `131` provider commands in order. The `FnOnce` command
ownership model prevents direct replay of the already immutable execution plan.

The next source goal is therefore a backend capability for reusable executables and
capture, keyed by immutable plan identity and typed dynamic shape, with stable dynamic
binding slots, fence-safe RAII ownership, bounded cache eviction, and an eager fallback
for unsupported commands or shapes. Dynamic admission and wait-for-capacity policy
remain independent and must not be replaced by fixed batch limits or rejection. The
legacy CUDA graph implementation is reference evidence only: its process-global cache,
per-replay upload, synchronization, hidden environment switches, and known Blackwell
failures are not suitable to copy into vNext. The remaining `16.83ms` device interval
means the new capability must measure both host submission reduction and device
launch-feed behavior before it can be a performance KEEP.

The copied artifact and machine-readable attribution are:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-submit-internal-2799db81-20260719/raw
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-submit-internal-2799db81-20260719/control/submit-internal-keep.json
```

The confirmed start-to-exited window was `850s`, approximately `$0.0934` at
`$0.39556/hour`. Instance `45218085` is verified at `cur_state=stopped`,
`actual_status=exited`, with no potentially billable sibling. This checkpoint does not
close formal S1, G01B, Metal correctness, release performance, or release readiness.

## Typed CUDA Replay Partial KEEP / Target REJECT At `beb3e63c`

Commits `eb4be067` through `beb3e63c` replace one-shot CUDA provider commands with
provider-declared replay contracts and a stream-local reusable-executable cache.
Initialization and dynamic binding remain explicit eager barriers. Captured entries
retain their command payloads, are bounded per stream, and are destroyed only after a
quiescent lane; capture failure paths terminate CUDA capture before either an eager
fallback or lane quarantine. Lazy preparation captures at most one new segment per
physically quiescent wave, while cache misses continue through the eager path without
rejecting or blocking the request. Typed profile counters expose candidate, captured,
cache-hit, rejected, replayed, and eager work without adding collection to the disabled
specialization.

Focused local validation passed with `CARGO_BUILD_JOBS=4` and
`RUST_TEST_THREADS=2`: the nine prepared-wave contracts, all-target checks for
`ferrum-interfaces`, `ferrum-kernels`, and `ferrum-models`, and the existing executor
metrics-schema test. Exact clean commit
`beb3e63c0e9b6daad9920f91efcbd0508a47ee69` was fetched through GitHub on the retained
RTX 4090 instance. The official CUDA feature command was:

```text
CARGO_BUILD_JOBS=8 cargo build --release -p ferrum-cli --bin ferrum --features cuda,vllm-moe-marlin,vllm-paged-attn-v2
```

The final release build took `6m45s`; binary SHA256 was
`d06ad248d0f8555a06877529ec7c077c1eb14e134e9670ea0c799126647b0204`.
Actual `ferrum run`, `ferrum serve`, streaming usage, and server smoke all passed before
performance was measured. The copied local artifact was revalidated and printed:

```text
FERRUM RUNTIME VNEXT S1 CUDA TRACE CHECKPOINT PASS: /Users/chejinxuan/ferrum-artifacts/runtime-vnext-replay-beb3e63c-20260719/raw
```

The performance diagnostic used Qwen3.5-4B, concurrency one, one isolated warmup
request, then two measured requests with 128 actual input tokens and 16 usage-counted
output tokens each. The measured requests completed `36` physical waves with zero
request errors, malformed streams, missing or duplicate `[DONE]`, zero-token outputs,
HTTP 500 responses, panics, or output-quality flags.

| Steady-state metric | `2799db81` eager baseline | `beb3e63c` replay | Change |
|---|---:|---:|---:|
| `DeviceRuntime::submit` per wave | `6.6515ms` | `2.1215ms` | `-68.105%` |
| ordered enqueue per wave | `6.6250ms` | `1.8190ms` | `-72.543%` |
| validate/prepare per wave | `0.0017ms` | `0.2785ms` | capture/cache lookup added |
| exact device-event execution | `16.8261ms` | `16.5587ms` | `-1.589%` diagnostic |
| output throughput | `22.4815 tok/s` | `22.9892 tok/s` | `+2.258%` diagnostic |

The measured replay counter differences were:

| Typed replay counter | Measured value |
|---|---:|
| candidate segments | `324` |
| cache-hit segments | `287` |
| newly captured segments | `6` |
| replayed segments | `293` (`90.432%` of candidates) |
| capture-rejected segments | `0` |
| replayed commands | `3987` |
| eager commands | `861` |
| replayed command share | `82.240%` |

This is an architecture KEEP because correctness held, CUDA rejected no capture, and
the attributed enqueue bottleneck fell by `72.543%`. It is a performance-target REJECT:
`1.8190ms/wave` misses the strict `<1.0ms/wave` threshold by `0.8190ms`, or `81.901%`
of the threshold. The one-repeat throughput movement is diagnostic only and is not a
same-hardware release performance claim.

The remaining failure class is now bounded. Approximately `24` commands per measured
wave remain eager at the token embedding and causal-attention dynamic-binding
boundaries. Six new captures during the measured requests also prove that physical
binding identity is not yet stable across otherwise identical requests. Before another
paid diagnostic, source work must provide a plan-owned stable device binding arena for
token, position, page-table, and sequence-control inputs, then split causal attention
into a dynamic-binding upload preamble and replayable compute segment. The next
artifact prediction is: zero measured-request captures, at least `95%` replayed
commands, no more than eight eager commands per wave, and enqueue strictly below
`1.0ms/wave`. No paid rerun is permitted until that typed contract and its local
phase/plan validation exist.

The copied artifacts and machine-readable decision are:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-replay-beb3e63c-20260719/raw
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-replay-beb3e63c-20260719/control/replay-diagnostic-reject.json
```

The confirmed running window was `36.6931` minutes, approximately `$0.2419` at
`$0.39556/hour`. Instance `45218085` is verified at `cur_state=stopped`,
`actual_status=exited`; the account inventory contains no potentially billable sibling.
This diagnostic does not close formal S1, G01B, Metal correctness, release performance,
or release readiness.

## Lane-Stable Replay Correctness PASS / Runtime-Capture Target REJECT At `a221e969`

Commits `63c212f5` and `a221e969` add lane-and-shape-stable dynamic backing, retain each
slot lease through the submission fence, and attach typed replay keys to token embedding
and causal-attention compute. The exact clean candidate was
`a221e96927e2c86a5c60aba64f8bf188b24d1a1a`; the CUDA release binary SHA256 was
`b91e67198f2e2191b5a8697e0dd271aa09227bd66826e157e0f6584fc42af944`. The build command
was:

```text
CARGO_BUILD_JOBS=8 cargo build --release -p ferrum-cli --bin ferrum --features cuda,vllm-moe-marlin,vllm-paged-attn-v2
```

Actual Qwen3.5-4B `ferrum run`, `ferrum serve`, streaming usage, and server smoke passed
before performance collection. The final validator and the post-performance validator
both printed:

```text
FERRUM RUNTIME VNEXT S1 CUDA TRACE CHECKPOINT PASS: /workspace/ferrum-artifacts/runtime-vnext-replay-a221e969-20260719/raw
```

The same-hardware diagnostic reused the `beb3e63c` workload and one RTX 4090 with driver
`590.48.01` and CUDA `12.4`: concurrency one, one warmup request, then two measured
requests with 128 random input tokens and 16 usage-counted output tokens. All `2/2`
measured requests completed and the error count was `0`.

| Measured metric | `beb3e63c` | `a221e969` | Target/result |
|---|---:|---:|---:|
| `DeviceRuntime::submit` per wave | `2.1215ms` | `3.5437ms` | diagnostic regression |
| ordered enqueue per wave | `1.8190ms` | `3.3396ms` | `<1.0ms` / REJECT |
| exact device-event execution | `16.5587ms` | `16.6283ms` | diagnostic only |
| replay command share | `82.240%` | `53.699%` | `>=95%` / REJECT |
| eager commands per wave | `23.917` | `66.056` | `<=8` / REJECT |
| captures after warmup | `6` | `23` | `0` / REJECT |
| output throughput | `22.9892 tok/s` | `26.6322 tok/s` | one-repeat diagnostic only |

The initial interpretation that the 23 captures proved continuing backing-address drift
was too strong. Wave arithmetic and resource snapshots correct that diagnosis:

- the warmup contained exactly `3` prefill plus `15` decode waves and produced exactly
  `18` captures;
- the measured interval contained `6` prefill plus `30` decode waves and produced `23`
  additional captures;
- the expanded replay surface exposes exactly `32` candidate segments per wave;
- dynamic-pool resident sizes and live-segment counts did not grow after warmup, and
  `backing_deferrals` remained `11`.

The bounded failure class is therefore `cuda_executable_cache_slow_population`. The
current request-path preparation captures and uploads one segment, then clears
`capture_allowed` because `cuGraphUpload` queued work on the owning stream. A request
with 32 candidate segments can consequently populate at most one missing executable per
wave. The measured interval includes graph construction and eager fallback instead of a
stationary replay path.

This is not fixed by increasing benchmark warmup or by removing explicit upload. The
repository records a Blackwell/CUDA 13 lazy-upload crash when first launch performs
implicit upload. The next architecture checkpoint must instead establish:

1. a typed reusable-execution `GraphPlan` derived from the immutable execution plan and
   documented runtime preset, with an explicit shape matrix and resource budget;
2. graph warmup/capture before `run` execution or `serve` readiness, shared by both
   product entrypoints rather than triggered by a user request;
3. quiescent two-phase preparation: capture and instantiate all admitted misses first,
   install their RAII owners, then explicitly upload before any launch;
4. request-time capture/JIT count `0`; the hot path may only replay a prepared entry or
   take a typed eager capability fallback;
5. cache size, graph-resource bytes, shape dispatch, capture count, eviction count, and
   fallback reason in typed health/profile evidence;
6. CPU-only policy tests for segment topology, multi-miss preparation, non-quiescent
   behavior, capacity protection, capture failure, upload failure, and observation
   aggregation before another paid CUDA run.

This direction keeps the stable backing and expanded replay surface as implementation
inputs, but rejects the current runtime-capture performance target. The next paid run is
not authorized until the startup plan and focused policy tests exist. Its falsifiable
prediction remains zero measured-request captures, replay command share `>=95%`, eager
commands `<=8` per wave, and enqueue `<1.0ms` per wave.

The copied artifact and machine-readable decision are:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-replay-a221e969-20260719/raw
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-replay-a221e969-20260719/control/performance-decision.json
```

Instance `45218085` was stopped after artifact copy-back and confirmed at
`cur_state=stopped`, `actual_status=exited`. Its final observed resumed uptime was
`18.54` minutes, approximately `$0.122` at `$0.39556/hour`; no running or scheduling
sibling existed. This checkpoint does not close formal S1, G01B, G06, performance, Metal,
or release readiness.

## Plan-Owned Reusable Bucket CUDA Correctness PASS At `b38e9645`

The focused CUDA artifact at source SHA
`e633acbb2a133e76d908e0ccbb77f295fe13f92e` was a correctness REJECT. Startup prepared
and retained `224` reusable executables, but the first `ferrum run` prefill failed with:

```text
invalid execution plan: dynamic pool growth exceeds its core-derived resident maximum
```

The failure proved that startup reusable-execution residency was resolved after
immutable memory provisioning and that a request could still manufacture a new exact
logical shape outside the plan-owned physical ceiling. The archived REJECT is:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-prefill-e633acbb-20260719
```

Commits `a5fcefa2`, `581c7bad`, `ae942763`, `7952e127`, and `b38e9645` replace that
relationship with:

- typed cache pressure, quiescent reclamation, and capacity deferral;
- an immutable reusable-execution policy and workspace budget in the memory plan;
- canonical class/capacity buckets selected from actual work without model, GPU, VRAM,
  CUDA, or Metal branches;
- one logical demand for the actual request and a distinct physical capacity shape for
  its selected bucket; and
- propagation of the bucket through Step, Invocation, submission-wave backing, lane
  identity, allocation, initialization, and evidence.

The focused local checks covered `ferrum-interfaces` and `ferrum-models` all-target
compilation, reusable-bucket membership/capacity/reuse, lane-stable pressure/reclaim,
operation prefix semantics, submission-wave contracts, device-operation-wave contracts,
and the model startup memory plan. These checks are bounded source evidence, not the CUDA
claim below.

The only retained Vast instance was reused because it held the exact source/build/model
caches. The paid lane used one RTX 4090 with UUID
`GPU-e3d7e4ce-fb55-2370-c54e-093ecf6fc635`, driver `590.48.01`, and the existing
Qwen3.5-4B HF snapshot. No model or binary was transferred from the workstation. The
official-feature release build completed in `7m11s`; binary SHA256 was
`5652a5daa13f214e7a175145a3163ab7b28eb2ddb185167605658585ef098e4a`.

The complete copied artifact and decisions are:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-bucket-b38e9645-20260719
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-bucket-b38e9645-20260719/control/bucket-binding-decision.json
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-bucket-b38e9645-20260719/control/same-hardware-comparison.json
```

The emitted health schema places `host_encode_submit_breakdown` beside
`host_encode_submit`. The prefill validator self-test had encoded it as a child instead.
Commit `b38465d1` fixes the validator and its mutation fixture to match the production
schema. A clean validator replay over the copied raw evidence printed:

```text
FERRUM RUNTIME VNEXT S1 CUDA TRACE CHECKPOINT PASS: /Users/chejinxuan/ferrum-artifacts/runtime-vnext-bucket-b38e9645-20260719/raw
FERRUM RUNTIME VNEXT PREFILL REPLAY CHECKPOINT PASS: /Users/chejinxuan/ferrum-artifacts/runtime-vnext-bucket-b38e9645-20260719/raw
```

The accepted correctness facts are:

- `ferrum run` exited `0` and returned exactly `Paris`;
- `ferrum serve` returned exactly `Paris`, one positive usage row, and exactly one
  `[DONE]`;
- `bench-serve` completed `4/4` requests with `0` errors and usage-counted output;
- run and serve produced `399` and `2,394` typed events respectively;
- all resource owners balanced, and the archived resident-growth error occurred `0`
  times; and
- no hidden Ferrum environment override selected the product behavior.

Startup prepared the declared prefill-64 and decode-32/16/8/4/2/1 descriptors. Request
traffic captured or uploaded no new executable. Decode replayed all `2,432/2,432`
candidate segments. Prefill replayed `320/512` candidate segments (`62.5%`); the other
`192` were explicitly classified outside preparation, with zero capacity defer, capture
rejection, or runtime capture. The replay checkpoint accepts this typed accounting, but
the `37.5%` outside-preparation share remains performance work.

The c=1 smoke reported `25.2366` output tok/s versus `24.1617` in the previous
same-instance artifact. The commands differ only in output paths, but both contain one
repeat and the old artifact failed `ferrum run` correctness. The `+4.45%` movement is
therefore diagnostic noise, not a performance claim. Current decode timing was:

| Decode boundary | Average per wave |
|---|---:|
| resource prepare attempt | `8.0742ms` |
| host encode/submit | `7.9670ms` |
| completion round trip | `14.9413ms` |
| submitted wave total | `24.7093ms` |

Source inspection found one concrete generic cause inside the first boundary: production
constructs the same canonical batch work shape inside the immutable-plan node loop,
performing participant binding, JSON serialization, SHA-256, and allocation up to `131`
times per physical wave. The next source checkpoint must bind this shape once per wave
and share its immutable authority across node requests. It must not weaken per-node
resource formula evaluation, participant/frame ownership, retry identity, or fence-held
lifetime.

Before another paid diagnostic, focused contracts must prove one shared shape authority
for a multi-node wave and unchanged node/participant fingerprints. The next same-hardware
prediction is decode `resource_prepare_attempt <=6.0ms` with both exact PASS lines above,
zero resident-growth errors, zero request-time captures, and unchanged release balance.
Missing the timing threshold classifies the candidate as REJECT and requires source
analysis before another GPU run.

The vNext resource/execution core currently contains no Qwen, Llama, MoE, CUDA, Metal,
ROCm, GPU-model, or VRAM-size branch. Single-device MoE and a new accelerator backend
must therefore add program resource formulas/capabilities and a `DeviceRuntime` provider,
not rewrite admission, recovery, ownership, defer/wake, or scheduling. Multi-device expert
parallelism may require one generic multi-device capacity-domain and collective-fence
extension. Legacy outer composition is not yet clean: the backend boundary audit still
rejects a CUDA string branch in `ferrum-cli::commands::serve`, and the legacy engine batch
path still contains a Qwen3MoE routing branch. Those are migration blockers, not reasons
to reintroduce model/platform policy into the resource core.

Instance `45218085` was stopped after artifact copy-back and is confirmed at
`cur_state=stopped`, `actual_status=exited`; the inventory contains no running or
scheduling sibling. This checkpoint closes the archived resident-ceiling blocker only.
It does not close formal S1, G01B, G04, Metal correctness, multi-model correctness,
release performance, or release readiness.

## Shared Work Authority CUDA Correctness PASS / Performance REJECT At `54fbadd5`

Commit `54fbadd5a7f4c61d9d70ed5b4da1da81e35355b0` constructs one immutable
`BatchWorkShape` for each physical submission wave and shares it across all `131` plan
node requests. The focused CUDA build used the official feature set and produced binary
SHA256 `341a99bdec0d8326f5a57851e6594d0e12be5114793ba2552a307aeef9cd8ce7`.
The same retained RTX 4090, UUID `GPU-e3d7e4ce-fb55-2370-c54e-093ecf6fc635`, driver
`590.48.01`, and CUDA `12.4` were used. Both validators printed:

```text
FERRUM RUNTIME VNEXT S1 CUDA TRACE CHECKPOINT PASS: /workspace/ferrum-artifacts/runtime-vnext-shared-work-54fbadd5-20260719/raw
FERRUM RUNTIME VNEXT PREFILL REPLAY CHECKPOINT PASS: /workspace/ferrum-artifacts/runtime-vnext-shared-work-54fbadd5-20260719/raw
```

`ferrum run` and `ferrum serve` both returned exactly `Paris`; streaming produced one
`[DONE]`; `bench-serve` completed `4/4` requests with zero errors and usage-counted
output. Run and serve retained `3` and `19` balanced resource owners respectively, and
the resident-growth error count was zero. Decode replayed `2,432/2,432` candidate
segments with zero request-time capture/upload; prefill retained the accepted
`320/512` replay plus `192` explicitly outside-preparation segments.

The source candidate is nevertheless a performance REJECT:

| Same-instance diagnostic | `b38e9645` | `54fbadd5` | Result |
|---|---:|---:|---:|
| decode resource prepare | `8.0742ms` | `9.0870ms` | `+12.54%`; misses `6.0ms` by `51.45%` |
| prefill resource prepare | `7.9156ms` | `8.2860ms` | `+4.68%` |
| decode wave identity bind | `2.0658ms` | `2.7406ms` | `+32.67%` |
| decode host encode/submit | `7.9670ms` | `10.4019ms` | `+30.56%` |
| one-repeat output throughput | `25.2366 tok/s` | `22.7031 tok/s` | diagnostic only |

This falsifies repeated `BatchWorkShape` construction as the dominant resource-prepare
bottleneck. Source inspection instead found that the physical all-node wave expanded
the same participant/frame authority into node-scaled session-flight owners while also
cloning participant/session/frame vectors into every prepared node. The next source
checkpoint therefore has two separate responsibilities:

1. share one immutable participant authority across every node projection in a wave;
2. track one session flight per participant for that physical wave, while preserving
   the exact `ParticipantNodeKey` topology registry, retired tombstones, per-node
   operation projections, and immutable full-plan coverage.

The local focused contracts for this checkpoint require `N` plan nodes, `P` participant
flight owners, and `N * P` operation/topology projections. Standalone node invocations
remain node-granular, and standalone and all-node wave flight owners must be mutually
exclusive. The current bounded source checks pass `25/25` session/frame cases, `10/10`
device-operation wave cases, `7/7` submission-wave resource cases, and the exhaustive
cancel contract. The existing executor trace now reports `wave_authorities`,
`covered_nodes`, `participant_flights`, and `node_participant_projections`, allowing the
next artifact to prove `N` nodes, `P` physical flight owners, and `N * P` correctness
projections directly. This is source evidence only, not a new CUDA performance claim.
The broader `vnext_source_audit_contract_tests` remains red on its existing whole-file
heuristic because unchanged `src/vnext/device.rs` contains both the constructor name
`fn unsupported` and unrelated successful `Ok(())` paths. The two other audit cases
pass; this checkpoint does not claim the broader source audit gate.

The copied artifact and machine-readable decision are:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-shared-work-54fbadd5-20260719
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-shared-work-54fbadd5-20260719/control/shared-work-decision.json
```

The focused paid window was approximately `20.15` minutes and `$0.13284`. Instance
`45218085` was stopped after artifact copy-back and verified at `cur_state=stopped`,
`actual_status=exited`, with no running or scheduling sibling. No further paid run is
authorized until the source checkpoint is committed and its typed owner/projection
counts are locally proven. This checkpoint does not close formal S1, G01B, G04, Metal,
multi-model correctness, release performance, or release readiness.

## Compact Full-Plan Wave Ledger Source Checkpoint

The next source layer removes a second node-scaled representation from full-plan waves.
Once preparation has proved that the requested nodes exactly equal the immutable plan in
plan order, the per-Step invocation registry no longer constructs, sorts, clones, and
inserts `N * P` `ParticipantNodeKey` values for that wave. It records one physical ledger
entry containing the wave topology fingerprint and the exact `N * P` coverage count.

This is a representation change, not weaker correctness ownership:

- operation identity remains projected for every node and participant (`N * P`);
- the physical session-flight owner count remains one per participant (`P`);
- the API now rejects participant-subset waves before demand evaluation or backing
  preparation, so one compact entry always covers the complete Step participant set;
- the compact ledger blocks every standalone node invocation because a full-plan wave
  covers every valid plan node in the Step;
- any prepared or retired standalone node entry blocks a later full-plan wave;
- definitely-not-submitted retry retains the same topology fingerprint while requiring a
  fresh physical invocation id; and
- retirement retains the compact full-plan tombstone, so completed topology cannot be
  prepared again in the same Step.

The executor trace adds `physical_ledger_entries` beside `wave_authorities`,
`covered_nodes`, `participant_flights`, and `node_participant_projections`. A healthy
full-plan wave must therefore report one physical authority, `N` covered nodes, `P`
participant flights, `N * P` operation projections, and one physical ledger entry.

Bounded local verification passes `26/26` session/frame tests, `11/11`
device-operation-wave tests, `7/7` submission-wave resource tests, the exhaustive cancel
contract, `ferrum-models --all-targets` compilation, and the executor topology-metric
test. These checks prove collision, retry, transition, retirement, capacity deferral,
reuse, cancellation, and typed evidence behavior for this source layer. They are not CUDA
performance evidence and do not change the archived `54fbadd5` performance REJECT. No
paid GPU is running.

## Plan-Compiled Resource Prepare Source Checkpoint

The `54fbadd5` CUDA artifact localizes the remaining first-order host cost to decode
`resource_prepare_attempt`: `9.0870ms` per wave against the `<=6.0ms` checkpoint target.
Source inspection found that every decode token still repeated immutable-plan work for
all `131` nodes:

- construct and clone a plan-node shape table;
- linearly match every plan node to an invocation-liveness row;
- linearly match every row resource to its descriptor;
- evaluate identical immediate, fit, and bucket shapes more than once;
- sort projections and rebuild their physical claim identity; and
- hash the complete stable bucket layout, then rescan requests and projections for every
  retained authority.

This source checkpoint compiles node/liveness/descriptor/projection relationships once
when `DynamicPoolSet` is constructed. The request path evaluates only the current dynamic
shape formulas and places results through precompiled integer indexes. It reuses the
plan-owned physical claim identity and omits duplicate formula evaluation when immediate,
fit, or bucket shapes are equal. A reusable bucket is now the typed stable-layout identity
inside one immutable plan; the first physical slot binds exact request/projection indexes,
and later claims validate those indexes in one linear pass without another SHA-256 or
nested lookup.

The optimization does not freeze capacity policy. Immediate and fit demand remain
request-derived; bucket capacity remains plan-derived; total-order and conservative
liveness packing are unchanged; exhausted pools still return typed deferral; release
wakes waiters; logical byte views remain exact; and backing ownership remains held through
the submission fence. The source core still has no model-family, CUDA, Metal, ROCm, GPU,
VRAM, or quantization branch.

Bounded local verification for this checkpoint passes:

- `reusable_bucket`: `3/3`;
- `lane_stable`: `2/2`, including busy-slot deferral and release/retry;
- device-operation wave contracts: `11/11`;
- submission-wave resource contracts: `7/7`;
- exhaustive device-operation cancellation: `1/1`; and
- `cargo check -p ferrum-models --all-targets`.

These are source-correctness checks, not a performance claim. The next paid diagnostic
must reuse the retained one-RTX-4090 environment, pass both CUDA trace/replay validators,
retain exact `ferrum run` and `ferrum serve` correctness, and report decode
`resource_prepare_attempt <=6.0ms`. A miss is a performance REJECT: copy the artifact,
stop billing, and inspect the remaining backing-claim validation and wave-identity work
before authorizing another CUDA run. The archived correctness PASS / performance REJECT
remains:

```text
/Users/chejinxuan/ferrum-artifacts/runtime-vnext-shared-work-54fbadd5-20260719
```
