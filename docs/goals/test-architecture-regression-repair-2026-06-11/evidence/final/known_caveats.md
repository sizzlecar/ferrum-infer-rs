# Known Caveats

- CUDA bench-serve evidence is a regression smoke with small prompt counts; it is not an official release performance claim.
- The L1 CUDA lane still records hb09/hb11 placeholders in its own note, so final hb09/hb11 evidence is taken from the separate product-path cuda-verify-live artifact.
