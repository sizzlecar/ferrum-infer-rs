# Repository Guidelines

## Project Structure & Module Organization
- This repository is a Rust workspace. Root configuration lives in `Cargo.toml`, with crates under `crates/`.
- Core contracts are in `crates/ferrum-types` and `crates/ferrum-interfaces`; implementations live in crates like `ferrum-engine`, `ferrum-models`, `ferrum-cli`, and `ferrum-server`.
- Integration tests are primarily in `crates/*/tests` (for example, `crates/ferrum-types/tests`).
- CI configuration is in `.github/workflows/ci.yml`; local runtime defaults are in `ferrum.toml`.

## Build, Test, and Development Commands
- `cargo check --workspace --all-targets` — fast compile validation across all crates/targets.
- `cargo build --workspace` — full workspace build.
- `cargo test --workspace` — run unit and integration tests.
- `cargo fmt --all -- --check` — verify formatting (same check used in CI).
- `cargo clippy --workspace --all-targets -- -A warnings` — advisory lint pass matching CI behavior.
- `cargo run -p ferrum-cli -- list` — run the CLI crate locally (swap `list` for `pull`, `run`, `serve`, etc.).

## Active M3 Performance Goal
- Current stretch target: `Qwen/Qwen3-30B-A3B-GPTQ-Int4` reaches at least `0.80 × vLLM` throughput for `c=1/4/16/32`; source of truth is `docs/bench/m3-80pct-goal-2026-05-25/GOAL.md`.
- Latest useful c=32 progress includes the 2026-05-28 VPA bridge: `FERRUM_USE_VLLM_PAGED_ATTN=1` now passes the Paris smoke and full-sweep same-pod N=3 improved throughput by about `+3.1%/+2.8%/+3.4%/+6.5%` for `c=1/4/16/32`; c=32 reached `1127.9 ± 101.4 tok/s` versus no-VPA `1058.9 ± 9.2 tok/s`.
- VPA is a correctness/unlock plus modest performance lever, not enough for the 0.80× target. A 2026-05-28 c=32 profile after VPA still shows MoE as the main decode bottleneck: active m=32 `batched-decode-prof` has roughly `~69%` MoE, `~11%` attention, `~14%` dense; within MoE, vLLM-Marlin gate_up/down GEMMs dominate.
- The 2026-05-28 session is a negative control: env-flip sweeps for `FERRUM_VLLM_MOE`, `FERRUM_MOE_GRAPH`, and `FERRUM_PAGED_FLASH_SPLITS` produced no new performance gain; do not repeat that strategy.
- A 2026-05-28 vLLM-source-inspired `moe_block_size=8` microbench is also a negative control: Qwen3-shaped c=32 all-active Marlin MoE regressed from about `224.7 µs` at block 16 to `247.4 µs` at block 8, so do not chase block-size 8 without a new full-model profile.
- The existing Triton fused-MoE PTX is not a viable drop-in for this M3 shape: `triton_fused_moe_bench_qwen3_shape` measured about `453 µs/layer` for gate_up only, versus the profiled vLLM-Marlin gate_up path at about `150 µs/layer`; do not port that path without a new kernel design.
- Skipping redundant vLLM-Marlin MoE workspace zeroing is correct but small: Paris passed and same-binary c=32 N=3 measured `1153.1 ± 152.7 tok/s` vs forced old zeroing `1130.4 ± 168.7 tok/s`; treat as a cleanup/modest win, not a primary lever.
- 2026-05-29 Codex handoff progress:
  - Fixed the default graph-clean path: `FERRUM_MOE_GRAPH=1` now defaults `FERRUM_VLLM_MOE=1`, and graph capture is gated off when the MoE path is not graph-clean. This fixes the multi-turn contamination seen in `ferrum run` without disabling the graph fast path.
  - Lowered the server MoE batched-decode default threshold to `FERRUM_MOE_BATCH_THRESHOLD=4`. c=4 no longer stays on the slow per-item path: current full sweep measured `425.6 ± 36.6 tok/s` at c=4 versus the pre-fix ~`122 tok/s` observation.
  - `FERRUM_VLLM_MOE_PAIR_IDS=1` is now a graph-clean default when vLLM MoE is enabled. Paris passed; same-pod N=3 measured c=16 `986.9 ± 10.2 tok/s` and c=32 `1249.5 ± 69.3 tok/s`, small but positive over the non-pair-id path.
  - Added the pair-id combine fast path (`weighted_sum_batched_f16`), not a residual fusion. Paris passed; valid same-pod N=3 artifact `/workspace/m3-graph-loop/bench_pairids_residual_c16_c32_n3_rerun2/` measured c=16 `993.8 ± 26.6` and c=32 `1264.0 ± 29.4` tok/s. Ignore `/workspace/m3-graph-loop/bench_pairids_residual_c16_c32_n3/`: the server was killed by process-group SIGINT during the run.
  - Reran vLLM 0.20.2 on the same pod for c=16/c=32 (`random 256/128`, N=3): `/workspace/m3-graph-loop/vllm0202_baseline_c16_c32_n3_retry/` measured c=16 `1328.7 ± 44.4`, c=32 `1971.8 ± 7.4` tok/s. Current Ferrum ratios are therefore about c=16 `0.75×`, c=32 `0.64×`; c=32 still needs roughly `+25%` throughput to clear 0.80×.
  - A partial vLLM 0.20.2 Marlin-MoE scheduling backport was tested and reverted: Paris passed, but c=16 `1000.9 ± 34.7` and c=32 `1250.4 ± 65.5` tok/s showed no c=32 gain. Do not repeat this partial tile/thread-config tweak without a new profiler reason.
  - A full-model `FERRUM_MOE_BLOCK_SIZE=8` validation was tested because vLLM 0.20.2 selects block size 8 for small-M MoE. Ferrum now has an override and a larger vLLM-MoE `c_tmp` scratch so block8 can be tested safely, but the result is negative: `/workspace/m3-graph-loop/block8_validation_rerun/` measured c=16 `975.1 ± 3.6`, c=32 `1209.5 ± 37.2`, below the block16 fast-path baseline. Do not make block8 default or repeat block8-only tests; revisit only as part of a full vLLM 0.20.2 Marlin template/source parity port.
  - Fresh current-default c32 profile after pair-id combine: `/workspace/m3-graph-loop/profile_current_pairid_combine_c32/`. With graph disabled for sync timers, steady m≈30/31 decode stays around `16–17 ms`; MoE is still `~64–66%`, and bucket timing is dominated by `gemm1≈6.2–6.6 ms` plus `gemm3≈3.0–3.2 ms`; combine is only `~0.25 ms`. Do not spend another primary lever on combine.
  - Clean restored-pod profile from clean checkout `c55e9c4`: `/workspace/m3-route-unified-layer-relaxed-clean-20260529_060400/`. Paris passed. For full c=32 decode, route shape was `batch_x_topk=256`, `block_size=16`, `total_post_pad=1040`, `active_blocks=65`, `unique_experts=61`. Real Marlin config is gate/up `prob_m=32 prob_n=1536 prob_k=2048 top_k=8` and down `prob_m=256 prob_n=2048 prob_k=768 top_k=1`; both use `thread_k=64`, `thread_n=128`, `threads=128`, `blocks_per_sm=3`.
  - The same clean profile confirms the active bottleneck: full c=32 decode median `unified total≈15.9 ms`, `model≈15.5 ms`, `decode_post≈0.35 ms`; `batched-decode-prof` median `total≈16 ms`, `moe≈10 ms` (`~62.5%`), attention≈2 ms, dense≈2 ms. Bucket timing is `gemm1≈5.86 ms` plus `gemm3≈2.89 ms` (`~97%` of bucket); combine is only `~0.25 ms`. Next primary lever should be MoE GEMM/source parity or a small-M fused MoE kernel, not scheduler/postprocess/combine/route sweeps.
  - Targeted Marlin thread-config A/B is a negative control: `/workspace/m3-marlin-thread-ab-n3-20260529_060835/` passed Paris for both rows, but c32 N=3 auto measured `1257.2 ± 50.2 tok/s` while forced `FERRUM_VLLM_MOE_THREAD_K=128 FERRUM_VLLM_MOE_THREAD_N=128 FERRUM_VLLM_MOE_BLOCKS_PER_SM=1` measured `1235.4 ± 35.1 tok/s` (`-1.7%`). The earlier c32 N=1 `+5.7%` was noise; do not default or shape-guard this config.
  - A vLLM 0.20.2 raw-op probe rules out block8 as the c32 source of vLLM's lead: `/workspace/m3-admin/vllm-raw-marlin-block8-block16-20260529_0624.json` used `/workspace/vllm-venv/bin/python` and synthetic `moe_wna16_marlin_gemm` at M=32/topk=8. On the same 114-block random route, gate_up block8/block16 measured `198.9/200.1 µs` and down measured `104.1/104.3 µs`; block8 halves npp but does not reduce block count or kernel time. Do not spend another loop on block-size-only work.
  - GPU process hygiene checkpoint: stale `m3build128x64d`/`ssh_tmux` 128x64 build jobs were killed, the dirty remote diff was saved to `/workspace/m3-admin/unknown-128x64-dirty-20260529_0620.diff` and stashed, and the final audit showed a clean remote checkout with no cargo/nvcc/ferrum/vLLM/tmux processes and an idle GPU. Local matching 128x64 edits were also stashed as `codex-save-local-unknown-128x64-dirty-20260529_0620`.
  - Use `scripts/m3_route_unified_profile.sh` on the restored pod to capture `[MOE_DUMP:*]`, `[vllm-moe-config]`, `[unified-prof]`, `[batched-decode-prof]`, `[iter-prof]`, and `[bucket-prof]` in one scoped c=32 run; it sets `FERRUM_MOE_GRAPH=0` because route dumping syncs/copies GPU buffers and fails fast if route shape, Marlin config, or unified timing is missing.
  - A short-context vLLM paged-attention v1 path is behind `FERRUM_VLLM_PAGED_ATTN_V1_SHORT` (default on, `=0` forces old v2). It removes the v2 reduce launch when `max_seq_len <= 512`. Local gates passed and restored-pod A/B `/workspace/m3-attn-v1-ab-codex-20260529_040601/` passed Paris for both paths: v1 c32 N=3 measured `1301.1 ± 25.0 tok/s` vs forced-v2 `1275.2 ± 56.7`, a small `+2.0%` directional win. Because this ran on a dirty remote binary where prompt-token-estimate was still default-on, use it only as same-binary attention A/B evidence, not as a clean current-default c32 row.
  - Vast instance `38237968` stopped during the route-dump build; later GPU work used a restored pod and tmux because background `nohup`/`setsid` jobs were killed by Vast SSH cgroups. Treat artifacts created during the outage/retry window as invalid unless they are explicitly listed below.
  - Restored GPU continuation after the credit issue used a dirty-main-equivalent remote binary rather than a clean checkout. Route/unified profile `/workspace/m3-moe-parity-lite-profile-20260529_033812/` passed Paris, captured real c=32 route shape `batch_x_topk=256`, `total_post_pad=832`, `active_blocks=52`, `unique_experts=48`, and showed graph-off steady decode median `total≈15.1 ms`, `model≈14.6 ms`, `decode_post≈0.34 ms`; postprocess/scheduler is not the primary gap.
  - The prompt-token-estimate scheduler candidate (`FERRUM_SCHED_PROMPT_TOKEN_ESTIMATE=1` in current code; it was default-on in the dirty binary used for this artifact) passed local scheduler unit test and GPU Paris. Stable c32 N=3 artifact `/workspace/m3-prefill-est-c32-stable-20260529_034726/` measured `1288.2 ± 13.0 tok/s`, `TPOT p50=20.1 ms`, `ITL p50=15.1 ms`, `TTFT p50=576 ms`. This is only a small improvement over `1264.0 ± 29.4`, inside the <10% A/B caution zone, and still about `0.65×` of same-pod vLLM c32 `1971.8 ± 7.4`; keep it opt-in until a direct same-binary A/B confirms.
  - Discard contaminated artifacts from the same continuation where delayed old scripts or tmux builds interrupted runs: `/workspace/m3-prefill-est-c32-20260529_033714/`, `/workspace/m3-prefill-est-c32-clean-*`, `/workspace/m3-prefill-est-c32-foreground*`, `/workspace/m3-prefill-est-c32-rerun-*`, and `/workspace/m3-prefill-est-c32-setsid-*`.
  - `FERRUM_MAX_BATCHED_TOKENS=4096` is a negative control for c32. Paris passed and N=1 artifact `/workspace/m3-mbt4096-c32-n1-20260529_035206/` measured `1304.9 tok/s` with `TPOT p50=19.0 ms`, but TTFT worsened to `723.7 ms`; do not make 4096 default or spend a primary loop on max-batched-token env sweeps without a new profile.
  - Added two scoped MoE debug/experiment aids for the next clean GPU build: `FERRUM_VLLM_MOE_LOG_CONFIG_LIMIT` / `FERRUM_VLLM_MOE_LOG_CONFIG_MIN_PAIRS` / `FERRUM_VLLM_MOE_LOG_CONFIG_MAX_PAIRS` let the Marlin config logger target the c=32 decode shape exactly, and opt-in `FERRUM_MOE_LARGE_M_BLOCK_SIZE` with `FERRUM_MOE_LARGE_M_MIN_PAIRS` tests larger Marlin block sizes only for large-M device-route work. Defaults are unchanged. Dirty-pod config probe `/workspace/m3-moe-config-probe-20260529_044441/` passed Paris and captured c32 route shape `batch_x_topk=256`, `total_post_pad=800`, `active_blocks=50`, `unique_experts=44`, but the old log cap only recorded smaller `batch_x_topk=104` calls selecting `thread_k=64`, `thread_n=128`, `blocks_per_sm=3`; treat it as debug evidence only, not a throughput claim.
  - A broad Marlin-MoE `{thread_k=128, thread_n=64, threads=128}` tile instantiation was tested and rejected: the larger binary loaded graph-on into 48GB OOM near layer 40. The instantiation was reverted. Do not add broad Marlin template instantiations without tracking binary/module/graph memory pressure.
  - Valid restored-pod current-binary vs pre-binary A/B with attention forced to old v2 (`FERRUM_VLLM_PAGED_ATTN_V1_SHORT=0`) passed Paris in all tested rows: `/workspace/m3-moe-parity-lite-binary-ab-trimmed-20260529_043153/` measured c32 current `1299.0 ± 48.4` vs pre `1244.4 ± 53.7` (`+4.4%`); `/workspace/m3-moe-parity-lite-binary-ab-c16-20260529_043419/` measured c16 `1001.5 ± 21.8` vs `999.9 ± 32.0`; `/workspace/m3-moe-parity-lite-binary-ab-c4-20260529_043830/` measured c4 `424.2 ± 14.1` vs `428.0 ± 35.1`; `/workspace/m3-moe-parity-lite-binary-ab-c1-20260529_044647/` measured c1 `161.9 ± 1.2` vs `160.6 ± 0.8`. Treat c32 as a real small win and lower rows as flat.
  - A scoped graph pre-sync removal was tested and reverted. Paris passed in `/workspace/m3-presync-skip-paris-20260529_074417`, but same-binary c32 N=3 `/workspace/m3-presync-ab-20260529_074643/` measured default skip-sync `1249.0 ± 27.3` vs forced old pre-sync `1252.6 ± 81.4` tok/s (`-0.28%`). Do not repeat graph-pre-sync removal as a primary lever.
  - A vLLM 0.20.2 DP + two-tile split-K Marlin-MoE kernel-body scheduler parity candidate was tested and reverted. It preserved Ferrum's EP/scale layout and only changed `marlin_template.h` slice scheduling. Paris passed all cells in `/workspace/m3-dp2sk-paris-20260529_083316`, but c32 single-smoke `/workspace/m3-dp2sk-c32-smoke-20260529_083702/` measured only `1262.0 tok/s`, `ITL p50=15.25 ms`, below the current c32 range. Static slice accounting also predicted only `0-8%` upside. Do not repeat this scheduler-only parity path as a primary lever.
  - Superseding raw-op check: Ferrum's direct C-ABI active65 Marlin-MoE microbench matches vLLM 0.20.2 on the clean c32 route shape (`batch_x_topk=256`, `active_blocks=65`, `total_post_pad=1040`). Artifacts: vLLM `/workspace/m3-admin/vllm-raw-marlin-active65-mulfalse-20260529_084848.json` measured gate/up `107.1 µs` and down `32.1 µs`; Ferrum `/workspace/m3-admin/ferrum-raw-marlin-active65-20260529_085145.json` measured gate/up `106.9 µs` and down `32.2 µs`. The earlier down-source-parity hypothesis is therefore not supported; graph-off bucket timers were measuring launch/sync/timer overhead around the kernel body. Next high-return work should target graph-on full-model coverage, engine scheduling, or a new fused MoE design only if a fresh profile exposes a multi-ms gap.
  - Graph-on production c32 profile `/workspace/m3-graph-prod-profile-20260529_085701/` passed Paris and measured c32 N=1 `1217.6 tok/s`. Steady full-batch snippets show `model≈12.5–14.6 ms`, `decode_post≈0.15–0.8 ms`, scheduler `≈20–44 µs`, and graph replay `upload≈0.10–0.13 ms`, `launch≈0.20–0.26 ms`; graph/post/scheduler are not a standalone 25% gap.
  - Prompt-token-estimate scheduler A/B is not default-worthy. Same-binary c32 N=3 `/workspace/m3-sched-prompt-est-ab-20260529_090018/` passed Paris for both rows and measured default `1278.6 tok/s`, `TTFT p50=374 ms`, `ITL p50=15.0 ms` versus `FERRUM_SCHED_PROMPT_TOKEN_ESTIMATE=1` `1314.0 tok/s` (`+2.8%`), `TTFT p50=603 ms`, `ITL p50=15.1 ms`. Keep it opt-in; do not run scheduler env sweeps without a new occupancy profile.
  - Unified chunked prefill (`FERRUM_UNIFIED_CHUNKED_PREFILL=64`) is a negative control from clean restored-pod testing. It implemented vLLM-style partial prefill in the unified path but was reverted after same-binary c32 N=1 `/workspace/m3-unified-chunked-prefill-ab-20260529_094117/`: both rows passed Paris, chunk64 measured `1219.9 tok/s`, `TPOT p50=18.27 ms`, `ITL p95=45.2 ms`, `TTFT p50=991 ms`; default measured `1232.6 tok/s`, `TPOT p50=22.62 ms`, `ITL p95=98.7 ms`, `TTFT p50=391 ms`. Throughput was `-1.03%` and TTFT regressed badly, so do not repeat chunk64 or chunk-size sweeps without first changing admission/fill policy and proving the TTFT cost is bounded.
  - Mixed-prefill profiling exposed a separate attention tail: graph-on c32 Ferrum still has `ITL p95≈95 ms` while same-pod vLLM is `≈14 ms`; graph-shape prewarm did not remove the tail because the large spikes are mixed/full prefill batches. Sync layer profiles measured vLLM-layout varlen attention around `~61 ms` versus legacy-layout varlen around `~46 ms` on the same mixed-prefill shape, with MoE around `~36 ms`. This makes vLLM-layout prefill attention a plausible large-gap area, but not via split-K.
  - vLLM-layout varlen split-K is a negative control. An opt-in `FERRUM_VLLM_VARLEN_SPLIT_K=1` candidate was implemented, built, tested, and reverted. The release build took `22m17s` because touching one attention `.cu` retriggered unrelated Marlin/MoE-Marlin CUDA template builds. Same-binary c32 N=1 artifact `/workspace/m3-vllm-varlen-splitk-ab-20260529_183142/` passed Paris for both rows, but split-K measured `1164.6 tok/s`, `TPOT p50=24.19 ms`, `ITL p95=104.57 ms`, `TTFT p50=419 ms`; default measured `1239.8 tok/s`, `TPOT p50=22.61 ms`, `ITL p95=96.68 ms`, `TTFT p50=383 ms`, so split-K was `-6.06%`. Do not reintroduce this two-phase varlen split-K path without a new kernel design; the likely path is a proper vLLM/FlashAttention-style tiled varlen prefill kernel.
  - Build-cycle efficiency is now a real blocker for kernel iteration: `ferrum-kernels/build.rs` reruns the whole CUDA build script and recompiles unrelated `vllm_marlin/*` and `vllm_marlin_moe/*` static libraries when a simple PTX kernel changes. Before another CUDA-kernel iteration, prefer adding build-script artifact/stamp caching or splitting these static libraries so unrelated Marlin templates are not rebuilt for attention-only experiments.
  - 2026-05-29 build-cache checkpoint: `ferrum-kernels/build.rs` now writes content-hash stamps for Marlin, vLLM-Marlin, vLLM-MoE-Marlin, and vLLM paged-attn static libraries. A one-time stamp generation build on the pod took `22m21s`; then an attention-only rebuild after touching `paged_varlen_attention_vllm.cu` took `3m15s`, and the final content-hash `-vv` verification took `194s`, with cache hits for all four static libraries and no Marlin/MoE-Marlin nvcc rebuild. Use this before the next tiled varlen prefill-attention experiment.
  - 2026-05-29 simple tiled-V varlen attention is a negative control. An opt-in `FERRUM_VLLM_VARLEN_TILED_V=1` candidate changed only the vLLM-layout varlen weighted-V phase to use 16-slot V tiles. It passed Paris, but same-binary c32 N=1 artifact `/workspace/m3-vllm-varlen-tiled-v-ab-20260529_115504/` measured `1165.0 tok/s`, `TPOT p50=23.88 ms`, `ITL p95=133.58 ms`, `TTFT p50=458 ms` versus default `1219.6 tok/s`, `TPOT p50=22.83 ms`, `ITL p95=120.40 ms`, `TTFT p50=392 ms`. The code was reverted. Do not repeat V-stage-only tiling; the attention path needs a full Q/K/V tiled FlashAttention-style design or a microbench-proven equivalent.
  - 2026-05-29 simple tiled-Q4 varlen attention is not a full-model lever. Standalone CUDA microbench for vLLM-layout varlen attention showed `tiled_q4` speedups of about `+23%` to `+36%` on prefill/mixed shapes with zero max_abs_err versus the reference bridge, so the opt-in `FERRUM_VLLM_VARLEN_TILED_Q4=1` path was added. GPU Git-flow validation at clean commit `73b1f58` passed Paris and c32 N=1 artifact `/workspace/m3-vllm-varlen-tiled-q4-ab-20260529_git_n1/` measured `1251.1 tok/s` versus default `1246.0 tok/s` (`+0.41%`), so do not default it or spend N=3 time on this simple Q-only tiling without a new profile showing prefill attention dominates.
  - 2026-05-29 vLLM FA2 paged-varlen probe identifies the next high-upside attention direction. New script `scripts/microbenches/vllm_flash_attn_varlen_probe.py` ran from clean Git on the pod and saved `/workspace/m3-vllm-fa2-varlen-probe-20260529_git.log`: vLLM `flash_attn_varlen_func` measured `40.35 µs` for `prefill_4x256`, `47.95 µs` for `mixed_3x256_4x1`, and `109.47 µs` for `prefill_4x512`. That is roughly an order of magnitude faster than Ferrum's simple vLLM-layout varlen bridge/q4 microbench, so the right primary attention lever is a real FA-style paged-varlen port or FA-compatible KV path. It is not a drop-in today because Ferrum's vLLM paged-decode K/V layout differs from vLLM FlashAttention's paged layout.
  - 2026-05-29 FA-compatible dual-layout KV probe is a real but modest opt-in win, not the final FA2 port. `FERRUM_FA_LAYOUT_VARLEN=1` keeps the primary vLLM paged-decode K/V pool for decode, writes a second legacy/FA-friendly `[block, slot, kv_head, head_dim]` pool for unified prefill/mixed varlen attention, and uses the existing legacy varlen attention reader on that pool. Clean Git commit `4e4c95d` passed local `cargo fmt --all -- --check`, `cargo check -q -p ferrum-cli`, `git diff --check`; remote release build took `3m11s` without CUDA static-lib rebuild. Same-binary restored-pod A/B passed Paris for all rows: c32 `/workspace/m3-fa-layout-varlen-ab-20260529_git_n3/` measured `1380.5 ± 34.7` vs default `1307.9 ± 26.6` tok/s (`+5.55%`), c16 `/workspace/m3-fa-layout-varlen-ab-c16-20260529_git_n3/` measured `1043.7 ± 25.5` vs `1010.3 ± 3.9` (`+3.30%`), and c4 `/workspace/m3-fa-layout-varlen-ab-c4-20260529_git_n3/` measured `432.7 ± 46.4` vs `419.8 ± 32.0` (`+3.09%`, noisy). Keep it opt-in for now: c1 was not run, the path allocates an extra per-layer K/V pool, and c32 still only reaches about `0.70×` of same-pod vLLM. The result does validate the FA-compatible-KV direction; the next attention lever should replace the simple legacy reader with a real FA-style paged/varlen kernel or wrapper.
  - 2026-05-29 simple Q4 tiling on the FA-compatible layout is a negative control and was reverted. Commit `0a1d99f` added `FERRUM_FA_LAYOUT_VARLEN_TILED_Q4=1`; remote release build took `3m14s`, and `/workspace/m3-fa-layout-tiled-q4-ab-c32-20260529_git_n1/` passed Paris for all rows, but c32 N=1 measured tiled `1346.3 tok/s` vs plain FA-layout `1364.4` (`-1.33%`) with worse `ITL p95` (`98.7 ms` vs `73.9 ms`). The code was reverted in `128103d`. Do not repeat Q-only tiling on this layout; the next attention attempt needs a real FlashAttention-style online softmax/tiled QK/V design or a proven wrapper.
  - 2026-05-29 naive online-softmax Q8 tiling on the FA-compatible layout is a negative control and was reverted. Commit `a889e65` added `FERRUM_FA_LAYOUT_VARLEN_FLASH_Q8=1`; remote release build took `3m14s` with no Marlin/MoE-Marlin nvcc rebuild. Artifact `/workspace/m3-fa-flash-q8-ab-c32-20260529_git_n1/` passed Paris for all rows, but c32 N=1 measured `flash_q8` `916.3 tok/s`, plain FA-layout `1367.4`, and default `1304.3`; `flash_q8` was `-33.0%` versus plain FA-layout and had much worse `ITL p95` (`222.0 ms`). Code and script row were reverted in `2cce5ab` / `7e7404a`. Do not repeat one-block-per-head Q8 online-softmax tiling; the attention path needs either a proven FA2 wrapper or a substantially different kernel design.
  - 2026-05-30 direct vLLM FA2 FFI probe is positive for raw wrapper feasibility, not yet a Ferrum integration. Commits `1c86c21` / `22226d0` added `scripts/microbenches/fa2_direct_ffi_probe.cpp`, which fills `Flash_fwd_params` and calls the `flash::run_mha_fwd` symbol exported by vLLM's `_vllm_fa2_C.abi3.so`, bypassing Python and `torch.ops`. Artifact `/workspace/m3-fa2-direct-ffi-probe-20260530.log` measured `prefill_4x256≈40.16 µs`, `mixed_3x256_4x1≈48.02 µs`, and `prefill_4x512≈109.46 µs`, matching the earlier Python FA2 probe and producing finite output. Caveat: the existing vLLM FA2 `.so` is a Python/Torch extension, so the probe must link `libtorch` and `libpython`, and it warns about `libcudart.so.12` from Torch versus `libcudart.so.13` from the FA2 extension. The next high-upside attention loop should be an opt-in C-ABI/dynamic shim around this path or a clean source-built FA2 wrapper; do not write another approximate one-block/tiled reader first.
  - 2026-05-30 opt-in Ferrum FA2 direct FFI integration is the first all-c path to clear the 0.80× target under a same-pod N=5 confirmation sweep. Commits `3e27afa` / `3d6aed0` add the runtime shim path (`FERRUM_FA2_DIRECT_FFI_SHIM=/workspace/libferrum_fa2_shim.so`) plus `scripts/m3_fa2_direct_ffi_ab.sh`; default builds and default runtime do not link Python/Torch. Follow-up `3b95ce9` makes `FERRUM_FA2_DIRECT_FFI_SHIM` alone enable the path, while `FERRUM_FA2_DIRECT_FFI=0` forces it off. Remote release build took `3m14s`; shim build artifact is `/workspace/m3-fa2-ferrum-shim-build-20260530.log`. Initial N=3 A/B showed the path was neutral at c1, modest at c4, and `+14.75%/+18.45%` at c16/c32 versus FA-layout. The final confirmation artifacts are committed under `docs/bench/cuda-rtx4090-2026-05-30-m3-80pct-confirmed/`: Ferrum FA2-direct N=5 measured c1 `160.4 ± 0.2`, c4 `446.3 ± 7.0`, c16 `1185.1 ± 12.3`, c32 `1641.9 ± 4.8`; same-pod vLLM 0.20.2 N=5 measured c1 `183.9 ± 0.2`, c4 `512.5 ± 2.8`, c16 `1331.9 ± 5.7`, c32 `1972.9 ± 18.6`. Ratios are c1 `0.872×`, c4 `0.871×`, c16 `0.890×`, c32 `0.832×`. Auto-shim smoke `/workspace/m3-fa2-auto-shim-smoke-20260530/` intentionally left `FERRUM_FA2_DIRECT_FFI` unset, passed Paris and a two-turn chat gate (`MULTITURN_CONTENT= Paris`), and c32 smoke completed `64/64` with 0 errors. Keep this as diagnostic evidence only: it depends on the vLLM/Torch FA2 extension and must not become the default or final M3 completion path. The next attention checkpoint must replace it with a source-built/Ferrum-owned FA2 wrapper or kernel with no vLLM/Torch runtime dependency.
  - 2026-05-30 source-built FA2 shim smoke validates the dependency-free direction but is not final/default. Commits `066bd7c` / `b7defce` added `scripts/microbenches/fa2_ferrum_source_shim.cu` and `build_fa2_ferrum_source_shim.sh`, compiling the hdim128 fp16 split-K FA2 forward templates directly from FlashAttention source into `/workspace/libferrum_fa2_source_shim.so`. `ldd` showed only system C/C++ libs and no vLLM/Torch/Python runtime link. Commit `6f640ab` added a multi-turn gate to the FA2 A/B runner. Remote smoke `/workspace/m3-fa2-source-shim-smoke-20260529_182244/` used `FA2_SHIM=/workspace/libferrum_fa2_source_shim.so` and `FA2_EXTRA_LD_LIBRARY_PATH=""`; both source-FA2 and FA-layout rows passed Paris and multi-turn. c32 N=1/64 prompts measured source FA2 `1553.7 tok/s` vs FA-layout `1310.8 tok/s` (`+18.53%`). Next step is in-repo `ferrum-kernels` source/static build integration plus N>=3/N=5 confirmation; do not count the runtime-loaded source shim as a default product path.
  - 2026-05-30 in-repo source-linked FA2 checkpoint: commits `2ba56af` / `dd18215` / `cb95d05` added the `fa2-source` feature, static `ferrum-kernels` link path, and fixed stub files under `crates/ferrum-kernels/kernels/fa2_source/stubs/`; `3c74fbb` / `6976f41` / `59f99d0` updated the A/B script and aligned `FERRUM_FA2_SOURCE` env parsing. Remote full release build on clean checkout took `28m32s`; after fast-forward to `59f99d0`, incremental rebuild took `3m14s`, and `ldd target/release/ferrum | grep -E "torch|python|vllm|fa2"` produced no output. Source-linked smoke `/workspace/m3-fa2-source-linked-smoke-20260529_192213/` ran with `FA2_SOURCE=1 FA2_EXTRA_LD_LIBRARY_PATH=""`, passed Paris and multi-turn for both rows, and c32 N=1/64 prompts measured source FA2 `1540.3 tok/s` versus FA-layout `1325.4 tok/s` (`+16.21%`). This removes the runtime shim dependency for the tested path, but it is still opt-in and not final/default: only c32 N=1 was run, the FA2/CUTLASS source dependency is still a build-time external checkout, and all-cell N>=3/N=5 confirmation is still pending.
  - 2026-05-30 stopped all-cell source-linked FA2 validation: commit `e07051f` added `scripts/m3_fa2_source_allcells_ab.sh`, a thin wrapper around the existing FA2 A/B runner for c=1/4/16/32. Remote partial artifact `/workspace/m3-fa2-source-allcells-n3-20260529_192908/` was stopped at user request during the c=4 source row; only c=1 completed. c=1 N=3 measured source-linked FA2 `160.45 ± 0.28 tok/s` versus FA-layout `160.73 ± 0.63` (`-0.17%`, effectively flat) and remains about `0.872×` of same-pod vLLM c1 `183.9`. This is useful no-regression evidence for c=1 only, not an all-cell performance claim; c4/c16/c32 source-linked confirmation remains missing. Remote processes were stopped cleanly and GPU returned to idle.
  - 2026-05-29 unified greedy argmax now uses the existing GPU row-argmax path when `FERRUM_GREEDY_ARGMAX=1`; `FERRUM_UNIFIED_GREEDY_ARGMAX=0` forces the old full-logits readback. Git-flow c32 N=1 artifact `/workspace/m3-unified-greedy-ab-20260529_git_n1/` passed Paris for both rows and measured unified greedy `1247.6 tok/s`, `TPOT p50=22.54 ms`, `ITL p95=83.67 ms`, `TTFT p50=295 ms` versus old readback `1235.6 tok/s`, `TPOT p50=22.45 ms`, `ITL p95=128.86 ms`, `TTFT p50=393 ms` (`+0.97%`). Keep the GPU argmax cleanup, but do not spend a primary N=3 loop on it unless later measurements show readback dominating.
  - 2026-05-29 follow-up correctness fix: unified greedy prefill results are now honored as token ids in the engine, and prefix-cache stores are skipped when prefix cache is disabled or the model returned a single-token greedy sentinel. Local gates passed (`cargo fmt --all -- --check`, `cargo check -q -p ferrum-engine`, `cargo test -q -p ferrum-engine --test continuous_batch_test`). Remote release build took `2m57s`; smoke `/workspace/m3-greedy-prefill-fix-smoke-20260529_git_n1/` passed Paris and measured c32 N=1 current default `1299.7 tok/s`, `TPOT p50=21.78 ms`, `ITL p95=85.19 ms`, `TTFT p50=325 ms`. Treat this as correctness/cleanup plus a current-default smoke, not a new N=3 performance claim.
  - 2026-05-29 prefill-first admission is not default-worthy. `FERRUM_SCHED_PREFILL_FIRST_UNTIL_ACTIVE=32` was added as an opt-in scheduler experiment to skip early decodes until the active decode cohort reaches 32. Git-flow c32 N=1 `/workspace/m3-prefill-first-ab-20260529_git_n1/` passed Paris and measured prefill-first `1287.1 tok/s`, `TPOT p50=20.47 ms`, `ITL p95=22.93 ms`, `TTFT p50=466 ms` versus default `1240.4 tok/s`, `TPOT p50=22.64 ms`, `ITL p95=82.92 ms`, `TTFT p50=320 ms` (`+3.77%`). With `FERRUM_SCHED_PROMPT_TOKEN_ESTIMATE=1` on both rows, `/workspace/m3-prefill-first-prompt-est-ab-20260529_git_n1/` regressed slightly: prefill-first `1267.0 tok/s` versus default `1274.4 tok/s` (`-0.58%`). It reduces tail spikes but does not deliver enough throughput and can worsen TTFT; keep opt-in only and do not spend N=3 without a stronger N=1 signal.
  - Clean Git-flow graph-on runtime profile `/workspace/m3-graph-runtime-profile-20260529_git_n1/` passed Paris and measured c32 N=1 `1309.3 tok/s`, `TPOT p50=21.17 ms`, `ITL p50=15.12 ms`, `ITL p95=117.9 ms`, `TTFT p50=360 ms`. Full-ish `items>=31` decode snippets had iteration median `14.9 ms`, model median `14.0 ms`, decode-post median `0.61 ms`; graph replay median was `13.16 ms` with upload+launch only `~0.35 ms`. The main c32 loss is not graph upload/launch or postprocess; it is fill/mixed-prefill tail behavior and remaining model work.
  - `FERRUM_ACTIVE_DECODE_PREFILL_CHUNK=64` is a new opt-in admission/fill experiment that chunks only prefills admitted while decode is already active; initial empty-queue prefills stay full-size. Local gates passed (`cargo fmt --all -- --check`, `git diff --check`, `cargo check -q -p ferrum-engine`, `cargo test -q -p ferrum-scheduler`, `cargo test -q -p ferrum-engine --test continuous_batch_test`, `cargo test -q -p ferrum-engine --test chunked_prefill_test`). Remote release build took `2m56s`; same-binary c32 N=1 `/workspace/m3-active-prefill-chunk-ab-20260529_git_n1/` passed Paris and measured active chunk `1305.0 tok/s`, `TPOT p50=15.87 ms`, `ITL p95=31.03 ms`, `TTFT p50=1065 ms` versus default `1304.3 tok/s`, `TPOT p50=21.19 ms`, `ITL p95=116.11 ms`, `TTFT p50=362 ms`. This is useful tail evidence but not a throughput lever; do not default it or run N=3 for M3 throughput unless a new policy bounds TTFT and improves tok/s.
  - Clean HEAD `8ef71ce` graph-on production profile `/workspace/m3-graphon-prod-profile-8ef71ce-20260529_120645/` passed Paris and measured c32 N=1 `1255.7 tok/s`, `TPOT p50=22.0 ms`, `ITL p95=127.0 ms`, `TTFT p50=393 ms`. Graph replay median was `total≈13.38 ms` with upload+launch only `≈0.32 ms`; sampled steady full-batch model calls stayed around `12–16 ms`. This reconfirms graph upload/launch, decode postprocess, and scheduler call overhead are not standalone 25% gaps.
  - Splitting mixed prefill+decode batches is not a primary lever. An opt-in `FERRUM_SPLIT_MIXED_PREFILL_DECODE=1` candidate routed mixed batches through existing pure-prefill plus pure-decode graph paths and was reverted after testing. Paris passed; c32 N=1 `/workspace/m3-split-mixed-ab-20260529_121843/` showed only `+2.4%`, and c32 N=3 `/workspace/m3-split-mixed-ab-n3-20260529_122047/` measured default `1256.3 ± 60.4` vs split `1274.7 ± 33.9` tok/s (`+1.46%`, overlapping noise). Do not repeat split-only mixed-batch work without a new profile.
  - Unified mixed/prefill layer-loop CUDA graph capture is a negative control. An opt-in `FERRUM_UNIFIED_MIXED_GRAPH=1` candidate captured only the Qwen unified layer loop and left H2D index upload, embedding, final norm/lm-head, and readback eager. It passed Paris, but c32 N=1 `/workspace/m3-unified-mixed-graph-ab-20260529_123549/` measured default `1250.4 tok/s` vs candidate `1202.7 tok/s` (`-3.82%`) with worse TPOT/ITL/TTFT. The code was reverted; mixed shape capture/replay is not worth repeating without evidence of stable repeated shapes.
- Current Ferrum full-sweep shape after the c=4 fix, before pair-id defaulting, was c=1 `155.4 ± 1.0`, c=4 `425.6 ± 36.6`, c=16 `965.8 ± 6.4`, c=32 `1205.6 ± 55.1` tok/s. Pair-id plus combine fast path replaces the c=16/c=32 rows with c=16 `993.8 ± 26.6`, c=32 `1264.0 ± 29.4`. Restored-pod current-binary A/B improved the c32 forced-v2 row to `1299.0 ± 48.4`, and short-attention v1 adds a separate same-binary `+2.0%` attention-path win. The opt-in FA-compatible dual-layout KV probe measured c4 `432.7 ± 46.4`, c16 `1043.7 ± 25.5`, and c32 `1380.5 ± 34.7`; the follow-up opt-in FA2 direct FFI N=5 confirmation measured c1 `160.4 ± 0.2`, c4 `446.3 ± 7.0`, c16 `1185.1 ± 12.3`, and c32 `1641.9 ± 4.8`. Against same-pod vLLM 0.20.2 N=5, this clears 0.80× at all four cells, but only through a diagnostic vLLM/Torch extension shim. A source-built no-vLLM-runtime shim smoke reproduced most of the c32 uplift at N=1/64 prompts (`1553.7` vs FA-layout `1310.8`, `+18.53%`) and passed multi-turn. The subsequent in-repo source-linked smoke also passed Paris and multi-turn with no vLLM/Torch/Python runtime link, measuring c32 `1540.3` vs FA-layout `1325.4` (`+16.21%`), but it is still only opt-in c32 N=1 and uses an external FA2/CUTLASS source checkout at build time. Prompt-token-estimate measured `+2.8%` but worsened TTFT and remains opt-in; unified chunked prefill was reverted after a negative c32 N=1, simple tiled-Q4 varlen attention was only `+0.41%`, unified GPU argmax was only `+0.97%`, prefill-first admission was only `+3.77%` before regressing with prompt-token-estimate, and active-decode-only prefill chunking was throughput-flat while worsening TTFT. Current default c32 remains around `0.66×`; the diagnostic FA2-direct c32 row is `0.832×`. The goal is not complete until the source-linked path gets all-cell N>=3/N=5 confirmation and a productized source dependency/defaulting decision.

## M3 Work Protocol
- Before using a paid GPU pod, state the single lever, expected gain, budget cap, stop condition, correctness gate, and benchmark command; get user approval if cost or runtime is material.
- Ferrum is the challenger. After locating the bottleneck, bias toward the highest expected throughput gain, not the lowest-risk cleanup. The required loop is: bottleneck localization → minimal GPU/CUDA validation → optimization patch → correctness gate → performance test → repeat.
- Work one high-return lever at a time, but do not wait idly: during long CUDA builds/tests, run non-overlapping source tracing, vLLM comparison, kernel review, or microbench design in parallel.
- Valid next levers are: full vLLM 0.20.2 Marlin-MoE source parity or a small-m fused MoE kernel, reduce vLLM-Marlin gate_up/down overhead with profiler evidence, rewrite/port `moe_align` only if fresh profiling shows route/align material, add block-table shared-memory cache in paged decode, or extend graph coverage beyond the MoE layer loop.
- Use vLLM source as the comparison baseline before inventing new kernels. For M3 benchmarking, inspect the `v0.20.2` tag first; HEAD-only findings are useful for ideas but not authoritative for the current ratio target.
- Do not run unscoped env-flip sweeps for M3 c=32. Current defaults already wire the cheap wins: `FERRUM_MOE_GRAPH=1` via `apply_moe_graph_default`, `FERRUM_DECODE_OP_PROFILE` is opt-in via `FERRUM_PROFILE_STAGES`, and c=32 paged decode chooses split-K=4.
- Correctness gates precede performance claims. At minimum run the Paris smoke/bisect for MoE or attention routing changes; never benchmark a path known to emit garbage except while fixing that bug.
- Performance claims need same-pod A/B and `N >= 3` for deltas under 10%; single Vast pod numbers can vary by about 10–17% from hardware, clocks, and neighbor load.

## Coding Style & Naming Conventions
- Follow Rust 2021 idioms and keep code `rustfmt`-clean.
- Formatting is defined in `rustfmt.toml`: 4-space indentation, max width 100, reordered imports/modules.
- Use `snake_case` for functions/modules/files, `CamelCase` for types/traits, and `SCREAMING_SNAKE_CASE` for constants.
- Keep crate boundaries clear: shared types/traits belong in `ferrum-types` or `ferrum-interfaces`, not duplicated in implementation crates.

## Testing Guidelines
- Prefer crate-local integration tests in `crates/<crate>/tests`.
- Use descriptive `snake_case` test names focused on behavior (example: `engine_status_serde_roundtrip`).
- Add tests for new public APIs, serialization changes, and scheduler/cache logic.
- Run `cargo test --workspace` before opening a PR.

## Release Regression Lessons
- Release readiness must cover both product entrypoints: `ferrum serve` / OpenAI API and `ferrum run`. A passing server Paris/multi-turn/concurrency gate does not prove the interactive CLI path works.
- `ferrum run` multi-turn correctness must be tested with default CLI sampling parameters and with explicit deterministic settings. Do not treat `--temperature 0` as a fix for garbage output; it is only a diagnostic.
- For MoE models, especially Qwen3-30B-A3B GGUF on Metal and GPTQ-Int4 on CUDA, verify the runtime preset actually selects the intended scheduler/KV path. Logs showing `priority scheduler` or `default KV cache manager` when the expected path is continuous batching / paged KV are release blockers.
- Every release candidate must include `run` multi-turn correctness and a basic `run` performance row in the saved regression evidence, not only HTTP benchmark artifacts.
- If any code changes after CUDA validation, rerun a CUDA quick regression for correctness, `run` multi-turn, and performance before calling the release ready.
- Do not publish or tag after a failed local product-path smoke. Fix the failed path, rerun Metal locally, rerun CUDA remotely when code changed, then record the evidence under `docs/bench/`.
- Do not validate release behavior by hand-assembling hidden env-var combinations that users will not know to set. Correctness, streaming, multi-turn, and README performance gates must exercise product defaults for `ferrum run` / `ferrum serve`; move required sizing/scheduler choices into typed auto-profile defaults or documented presets.
- Do not hard-code model-family prompt hacks such as forced empty Qwen3 `<think>` blocks. Prefer model-provided chat templates from GGUF/HF metadata, and make template rendering shared between `run` and `serve`.
- Do not hide invalid model output by filtering decoded text. If `<unk>`, `[PAD...]`, or tokenizer-reserved IDs appear, trace token IDs and fix sampling/logit masking or KV/logits state before accepting the regression.

## Release Gate Policy
- `cargo test --workspace --all-targets` is the first source gate.
- Metal source releases must use `scripts/metal_readme_regression.py` plus `scripts/release/validate_metal_readme_regression.py`; do not replace this runner with a private all-in-one script.
- CUDA source releases must use `scripts/m3_ab_runner.py` plus `scripts/m3_validate_runner_artifact.py` with the G0 configs under `scripts/release/configs/`.
- Official release asset validation must use `scripts/release/release_binary_gate.py` for Metal tarball, CUDA tarball, Homebrew Metal, and Homebrew CUDA fetch gates.
- Every release gate must print an explicit `PASS` line and return non-zero on failure. A silent successful command is not enough release evidence.
- If a change touches shared model, scheduler, runtime default, tokenizer/template, CLI, or server code, both Metal and CUDA must be regressed. Backend-local CUDA changes require CUDA full/smoke plus a Metal smoke; backend-local Metal changes require Metal full plus a CUDA smoke.

## G1-G4 Local Goal Policy
- The local G1-G4 goal does not require tagging, publishing, cargo release, Homebrew release, or GitHub release unless the user explicitly asks for those steps again.
- G1 vLLM migration compatibility is a local product/API gate and should not open a paid GPU. Use `scripts/release/g1_vllm_migration_gate.py` and save artifacts under `docs/release/g1-g4/g1-vllm-migration/<timestamp>-<short_sha>/`.
- G1 must preserve real CLI semantics for vLLM-style flags: `--max-model-len` maps to `FERRUM_MAX_MODEL_LEN`, `--max-num-seqs` maps to `FERRUM_PAGED_MAX_SEQS`, `--max-num-batched-tokens` maps to `FERRUM_MAX_BATCHED_TOKENS`, and prefix-cache flags map to `FERRUM_PREFIX_CACHE`.
- Do not add ignored compatibility flags. If Ferrum cannot implement a vLLM flag, document it in `docs/migration/vllm-compatibility-matrix.md` instead of silently accepting it.
- G1 acceptance requires `ferrum serve --help` visibility, effective-config JSON entries sourced from `cli`, OpenAI non-stream and stream smoke, exactly one stream `[DONE]`, exactly one usage chunk before `[DONE]`, and a final `G1 VLLM MIGRATION PASS: <artifact_dir>` line.
- G2 agent-serving compatibility covers strict JSON schema, `response_format`, tools, `tool_choice`, and OpenAI-shaped streaming behavior. Any change touching these paths must run `cargo test -p ferrum-server structured_output` plus the G2 CLI real-model tests before claiming completion.
- G2 `tool_choice=required` semantics are product behavior: require at least one function tool, steer toward a tool argument schema, return OpenAI-shaped `tool_calls`, and return/stream an OpenAI-shaped error with `param=tool_choice` if no valid tool call is produced.
- G2 must not leak invalid strict-schema or required-tool content before validation in streaming mode. Buffer first; then emit valid deltas or an OpenAI-shaped SSE error followed by exactly one `[DONE]`.
- G2 final evidence must print `G2 AGENT SERVING PASS: <artifact_dir>` and record artifacts under `docs/release/g1-g4/g2-agent-serving/<timestamp>-<short_sha>/`.
- G3 prefix/session cache changes cover `FERRUM_PREFIX_CACHE`, `FERRUM_SESSION_CACHE`, `X-Ferrum-Session`, KV/prefix sharing, request session ids, scheduler admission, `/health.cache`, and cache metrics. Any change touching these paths must run G3 cache correctness tests.
- G3 correctness is mandatory: prefix cache must not change greedy byte output for identical requests, must not cross-talk shared-prefix suffixes, and must not corrupt strict JSON or tool calls. Session cache must be opt-in and must not leak content across different session ids.
- G3 performance claims require `ferrum bench-serve --fail-on-error --require-ci` shared-prefix artifacts. If TTFT improvement is inconclusive, the artifact may say so, but cache hits and saved prefill token counters must still be present and correctness must pass.
- G3 final evidence must print `G3 CACHE PRODUCT PASS: <artifact_dir>` and record artifacts under `docs/release/g1-g4/g3-cache-product/<timestamp>-<short_sha>/`.

## Directory Cleanup Policy
- Before moving, deleting, or archiving files under `crates/`, `docs/`, or `scripts/`, run `python3 scripts/release/inventory_tree.py --out docs/release/cleanup/<YYYYMMDD>-inventory.md` and commit the inventory.
- Do not delete benchmark or release evidence unless the cleanup manifest lists the file, category, reference count, and reason.
- Keep workspace crate membership in `Cargo.toml` synchronized with actual `crates/*/Cargo.toml` directories.
- Prefer adding `scripts/README.md` classifications and archive candidates before doing physical moves; avoid large script-layout churn in the same PR as gate logic changes.

## bench-serve Policy
- `ferrum bench-serve` is the canonical HTTP performance client for `/v1/chat/completions`; do not add a second release throughput path for the same endpoint.
- Release performance claims must use streaming with `stream_options.include_usage=true`, require `output_token_count_source=\"usage\"`, and run with `--fail-on-error --require-ci --seed 9271`.
- Smoke runs may omit `--require-ci`, but must still use `--fail-on-error`.
- A streaming request is successful only when it receives `data: [DONE]`, emits at least one output token, and has no stream/JSON error.
- Reports must use actual tokenizer-counted input lengths for `random`, `sharegpt`, and `shared-prefix` prompts; do not reuse `random_input_len` for non-random datasets.

## GPU Cost Policy
- CUDA G0 gates target one RTX 4090 and `Qwen/Qwen3-30B-A3B-GPTQ-Int4`; do not broaden paid GPU release gates to multiple GPUs or multiple CUDA models without explicit approval.
- Before using a paid GPU, state the lane, expected runtime/cost, stop condition, correctness gate, and performance command.
- Do not leave paid GPU instances idle. Stop or destroy them after validation evidence is collected unless the user explicitly asks to keep them running.
- If CUDA validation fails, collect the exact failing artifact/log, stop unnecessary processes, and avoid repeated full sweeps until the failure mode is understood.

## Commit & Pull Request Guidelines
- Follow the existing commit style: conventional prefixes plus scope when useful, e.g. `feat(cli): ...`, `refactor(engine): ...`, `feat(cli, models): ...`.
- Keep commits focused and imperative; avoid mixing unrelated crates in one commit when possible.
- PRs should include: purpose, affected crates, key design notes, and validation steps/commands run.
- Link related issues and include sample CLI/API output when behavior changes.
